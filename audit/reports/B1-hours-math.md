### F-B1-1 | S0 | confidence: high
**Claim:** `assertNoOverlap` only loads same-week entries by `local_date`, so overlapping clock spans on opposite sides of a Monday week boundary are both counted in two weekly totals.
**Location:** `apps/api/src/domains/timesheet/services/timesheetCommandService.ts:732-762`, `apps/api/src/domains/timesheet/repositories/timeEntryRepository.ts:205-217`, `supabase/migrations/017_time_tracking.sql:108-110`
**Trace:** `POST /api/v1/time-entries/retroactive` (or `/:id/clock-out`) → `timesheetCommandService.createRetroactiveEntry` / `clockOut` → `assertNoOverlap` → `timeEntryRepository.listForCarerWeek(householdId, carerId, weekStart, weekEndExclusive)` → half-open `local_date` filter → `rollUpIntoTimesheet` → `sumWorkedMinutes` → `timesheets.total_minutes`.
**Wrong-number scenario:** Household `Europe/London`, carer A. (1) Retroactive entry `clock_in_at=2026-07-26T21:00:00Z`, `clock_out_at=2026-07-27T01:00:00Z` → `local_date=2026-07-26`, 240 worked minutes, rolled into `week_start=2026-07-20`. (2) Second entry `clock_in_at=2026-07-27T00:00:00Z`, `clock_out_at=2026-07-27T08:00:00Z` → `local_date=2026-07-27`, 480 minutes, rolled into `week_start=2026-07-27`. Step (2)’s overlap check lists only `local_date >= 2026-07-27`, so entry (1) is invisible despite sharing 2026-07-27T00:00–01:00Z. Reported totals: 240 + 480 = 720 minutes; true unique time: 660 minutes (60 minutes double-counted across two timesheets).
**Fix sketch:** In `assertNoOverlap`, load completed entries whose clock span intersects `[clockInAt, clockOutAt)` (time-range query or widen the `local_date` window by ±1 day and filter in memory), not only those whose `local_date` falls in the clock-in week.

### F-B1-2 | S0 | confidence: high
**Claim:** Paid-cancellation entries stamp `local_date` in the shift’s authoring timezone but the weekly roll-up buckets by household timezone, so cancellation hours can be written yet omitted from `timesheets.total_minutes`.
**Location:** `apps/api/src/domains/timesheet/services/timesheetCommandService.ts:511-518`, `546`, `1160-1172`, `supabase/migrations/017_time_tracking.sql:108-110`, `apps/api/src/domains/shift/services/shiftChangeRequestCommandService.ts:719-733`
**Trace:** `respond` on a paid cancel → `recordCancellationPaidEntry` → `timeEntryRepository.createSubmitted({ timezone: shift.timezone, … })` → DB trigger `sync_time_entry_local_date` → `rollUpIntoTimesheet` → `weekStartOf(clock_in_at, household.timezone)` → `listForCarerWeek` by `local_date` → `sumWorkedMinutes` → `timesheets.total_minutes`.
**Wrong-number scenario:** Household timezone `UTC`; shift `starts_at=2026-08-03T01:30:00Z`, `ends_at=2026-08-03T09:30:00Z`, `timezone=America/Los_Angeles`, `cancellation_paid=true` (480 scheduled minutes). Entry is created with `local_date=2026-08-02` (Sunday in LA) but roll-up uses `week_start=2026-08-03` (Monday in UTC). `listForCarerWeek` requires `local_date >= 2026-08-03`, so the new row is excluded; `total_minutes` stays at the pre-cancel sum (e.g. 450) instead of 930. The row exists in `time_entries` but not in the approved weekly total.
**Fix sketch:** Write cancellation entries with `timezone: household.timezone` (or recompute `local_date` with household zone before roll-up); alternatively filter `listForCarerWeek` by clock-in instant week, not `local_date` alone.

### F-B1-3 | S0 | confidence: high
**Claim:** In a two-carer household, the Hours UI sums every carer’s entries but attaches one arbitrary carer’s timesheet/earnings, so the header total can disagree with the timesheet row and with `earnings.worked_minutes`.
**Location:** `apps/api/src/domains/timesheet/repositories/timeEntryRepository.ts:174-185`, `apps/api/src/domains/timesheet/services/timesheetQueryService.ts:87-99`, `apps/mobile/src/hooks/queries/useWeekTimeEntries.ts:25-26`, `apps/mobile/src/api/endpoints/timesheets.ts:90-97`, `apps/mobile/src/domains/timesheet/components/ParentWeekView.tsx:245-249`, `apps/mobile/src/domains/timesheet/components/NannyWeekView.tsx:193-194`
**Trace:** `GET /api/v1/households/:householdId/time-entries?week_start=` → `timesheetQueryService.listForHouseholdWeek` (no `carer_id` filter) → mobile `sumEntryMinutes(entries)` for `hours-total`; parallel `timesheetApi.getWeek` → `list(householdId)` → `.find(t => t.week_start === weekStart)` (first match only) → `GET /api/v1/timesheets/:id` → `getWeekWithEarnings` → `weekEarningsService.computeForWeek` for that one `carer_id`.
**Wrong-number scenario:** Household with carers B (480 min) and C (300 min) in week `2026-08-03`. API returns both entry sets (900 min if summed). `timesheets` has two rows with `week_start=2026-08-03`; `getWeek` returns whichever `list()` returns first (unordered among same `week_start`). UI shows `hours-total` = 780m (B+C) while `timesheet.total_minutes` and `earnings.worked_minutes` reflect only one carer (480m or 300m). Parent approves the wrong carer’s sheet while staring at a combined-hours header.
**Fix sketch:** Scope week entry queries and mobile sums to one `carer_id`, and resolve timesheets with `(household_id, carer_id, week_start)` instead of `week_start` alone.

### F-B1-4 | S1 | confidence: med
**Claim:** After a household timezone change, `rollUpIntoTimesheet` re-buckets weeks using the new household zone while `time_entries.local_date` stays frozen in the old entry timezone, so an entry can drop out of the recomputed weekly sum.
**Location:** `apps/api/src/domains/timesheet/services/timesheetCommandService.ts:280-290`, `1160-1172`, `supabase/migrations/017_time_tracking.sql:108-120`, `apps/api/src/domains/timesheet/repositories/timeEntryRepository.ts:216-217`
**Trace:** `PATCH /api/v1/households/:householdId` (timezone) → later `clockOut` / `updateEntry` → `rollUpIntoTimesheet` → `householdRepo.findById` (new timezone) → `weekStartOf(entry.clock_in_at, newTimezone)` → `listForCarerWeek` filtered on `local_date` derived at insert from old `entry.timezone` → `sumWorkedMinutes`.
**Wrong-number scenario:** Entry clocked in under `America/New_York` with `clock_in_at=2026-08-04T04:00:00Z` → `local_date=2026-08-04`, `timezone=America/New_York`. Household timezone is later changed to `America/Los_Angeles`. On clock-out, roll-up uses LA calendar for the same instant (`local_date` would be `2026-08-03`) but `listForCarerWeek` for the LA-derived `week_start` excludes `local_date=2026-08-04`. `total_minutes` recomputes without that session (e.g. drops from 480 to 0) while the entry row still shows 480 minutes worked.
**Fix sketch:** Bucket roll-ups with `weekStartOfLocalDate(entry.local_date)` (or persist `week_start` on the entry at write time) instead of re-deriving week from `household.timezone` at roll-up time.
