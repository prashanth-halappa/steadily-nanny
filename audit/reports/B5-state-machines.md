### F-B5-1 | S0 | confidence: high
**Claim:** Accepting a paid cancellation succeeds while swallowing the `week_approved` guard, leaving `shifts.cancellation_paid = true` with no `cancellation_paid` time entry and no pay for those hours.
**Location:** `apps/api/src/domains/shift/services/shiftChangeRequestCommandService.ts:777-790`; `apps/api/src/domains/timesheet/services/timesheetCommandService.ts:524-526`; `apps/api/src/domains/timesheet/services/timesheetCommandService.ts:487-489`
**Trace:** `POST /change-requests/:id/respond` → `shiftChangeRequestCommandService.respond` (accept + `CANCEL`) → `accept_shift_change_request` sets `cancellation_paid` on the shift (`supabase/migrations/029_accept_shift_change_request_events.sql:118-130`) → `cancellationPaidEntryRecorder` → `timesheetCommandService.recordCancellationPaidEntry` throws `TimeEntryNotEditableError` when `timesheet?.status === 'approved'` → caught and logged only in `respond`, which still returns success.
**Wrong-number scenario:** Week `2026-08-04` is already `approved`. A Mon 09:00–17:00 shift (480 scheduled minutes) is short-notice cancelled with paid cancellation. Accept returns 200; shift row shows `cancellation_paid = true`. No `time_entries` row with `kind = 'cancellation_paid'` is created. Earnings for that week never include 480 paid-cancel minutes — e.g. at £15/hr (`rate_minor = 1500`) the carer is underpaid by £120.00 vs the cancellation policy.
**Fix sketch:** In `respond`, treat `TimeEntryNotEditableError` with `week_approved` as a hard failure (or reopen the week before recording); do not catch-and-continue after a successful cancel RPC.

### F-B5-2 | S0 | confidence: high
**Claim:** Parent `PATCH /shifts/:id` bypasses the shift immutability guard and can rewrite times on a shift that already has `time_entries` (including while a carer is still `running`), corrupting `scheduled_minutes` frozen at clock-out.
**Location:** `apps/api/src/domains/shift/services/shiftCommandService.ts:138-179`; `apps/api/src/domains/shift/repositories/shiftRepository.ts:267-301`; `supabase/migrations/034_parent_shift_edit_demote_on_time_change.sql:27-52`; contrast `apps/api/src/domains/shift/repositories/shiftRepository.ts:76-87` and `supabase/migrations/028_open_shift_change_request_immutability.sql:43-54`
**Trace:** `PATCH /shifts/:shiftId` → `shiftCommandService.update` (no `assertMutable`) → `shiftRepository.applyParentEdit` → RPC `apply_parent_shift_edit` (locks row, updates times; no `completed`/`cancelled`/time-entry check). Change-request paths use `assertMutable` + RPC immutability checks instead.
**Wrong-number scenario:** Carer clocks into a 09:00–17:00 shift (`running` entry exists). Parent PATCHes the same shift to 09:00–13:00. Carer clocks out at 17:00 with 480 worked minutes; `freezeScheduledMinutes` (`timesheetCommandService.ts:1126-1140`) reads the edited 4h span and stores `scheduled_minutes = 240`. Worked vs scheduled diverge; closure/guaranteed-top-up logic that recomputes from shift spans (`weekEarningsService.ts:298-302`) can price the wrong scheduled baseline.
**Fix sketch:** Call `shiftRepo.assertMutable(shiftId)` before `applyParentEdit`, or add the same `completed`/`cancelled`/time-entry checks inside `apply_parent_shift_edit`.

### F-B5-3 | S1 | confidence: high
**Claim:** Co-parent approval timeout marks a row `timed_out` and runs the applier, but applier failures are logged and skipped, so silence can look like approval while the gated mutation never runs.
**Location:** `apps/api/src/domains/household/repositories/coParentApprovalRepository.ts:84-110`; `apps/api/src/domains/household/services/coParentApprovalQueryService.ts:58-71`; `apps/api/src/domains/household/services/approvalApplierRegistry.ts:69-86`; `apps/api/src/jobs/scheduleHorizonJob.ts:113-124`
**Trace:** Daily `scheduleHorizonJob` → `expirePendingApprovals()` → `expireTimedOut` flips `pending` → `timed_out` → `approvalApplierRegistry.applyAllSettled` catches per-row errors. Explicit respond path (`coParentApprovalCommandService.ts:139-141`) propagates applier failure; timeout path does not.
**Wrong-number scenario:** Parent A queues a short-notice paid cancel (`approval_scope` + `ask_other`). Before timeout, the carer clocks into the shift (shift now has `time_entries`). Timeout fires → approval becomes `timed_out` → `applyApprovedChangeRequest` hits `shiftRepo.assertMutable` (`shiftChangeRequestCommandService.ts:461`) and throws → error swallowed. Parent A believes silence approved the cancel; shift stays active; no `cancellation_paid` entry; paid-cancel hours never land on the timesheet.
**Fix sketch:** On applier failure after `timed_out`, flip approval to a terminal failure state and surface to the requester; or retry until mutable / notify manual action.

### F-B5-4 | S1 | confidence: med
**Claim:** `co_parent_approvals.status = 'withdrawn'` is in the CHECK constraint but no write path sets it, so a requester cannot cancel a pending approval and must wait for timeout—which auto-applies the gated mutation.
**Location:** `supabase/migrations/022_co_parent_approvals.sql:17-19`; `apps/api/src/domains/household/services/coParentApprovalCommandService.ts:74-100`; `apps/api/src/domains/household/routes/householdApprovalRoutes.ts` (list + respond only, per `audit/reports/A1-route-map.md:96-97`)
**Trace:** Gate creates `pending` row (`approvalGateService.ts:84-92`). Terminal exits: `respond` → `approved`/`declined` (`coParentApprovalRepository.ts:47-74`), or `expireTimedOut` → `timed_out` + applier (`coParentApprovalRepository.ts:88-94`). No route/service calls `withdrawn`.
**Wrong-number scenario:** Parent A opens co-parent-gated extra shift for Tue 10:00–18:00, then realizes the slot is wrong. Co-parent B never opens the app. After `approval_timeout_minutes`, timeout auto-applies (`022` header; `scheduleHorizonJob.ts:107-108`) and creates the unwanted pending shift anyway—no user-facing way to withdraw the queue item.
**Fix sketch:** Add requester-only `withdraw` on pending approvals, writing `status = 'withdrawn'` without running the applier.

### F-B5-5 | S2 | confidence: med
**Claim:** Pending `shift_change_requests` have no timeout or expiry; if the responder never acts, the request stays `pending` indefinitely with no automatic resolution.
**Location:** `supabase/migrations/015_shifts.sql:161-163`; `apps/api/src/domains/shift/services/shiftChangeRequestCommandService.ts:844-877`; `apps/api/src/jobs/scheduleHorizonJob.ts` (no shift-change-request sweep)
**Trace:** `create` → `open_shift_change_request` inserts `status = 'pending'` (`030_open_shift_change_request_events.sql:107-123`). Terminal transitions: `respond` → `accepted`/`declined`, `withdraw` → `withdrawn` (`shiftChangeRequestRepository.ts:243-246`), supersede on new open (`030:96-105`). No job or column ages out stale pending rows.
**Wrong-number scenario:** Parent opens a cancel request for Sat 09:00–17:00; nanny never responds. Shift remains `confirmed` on the calendar indefinitely; parents cannot infer cancellation; carer may still clock in and accrue hours for a shift the household thought was in limbo.
**Fix sketch:** Add `expires_at` + horizon-job sweep to `withdrawn`/`declined`, or document and enforce responder SLA in product—not a money bug by itself, but a stuck lifecycle state with payroll ambiguity.
