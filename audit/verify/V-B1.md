### F-B1-1 — CONFIRMED
**Verdict basis:** Opened `timesheetCommandService.ts:732-762` (`assertNoOverlap` → `listForCarerWeek` only), `:340-357` (`clockOut` week + overlap), `:411-437` (`createRetroactiveEntry` `CLOCK_CROSSES_WEEK`), `timeEntryRepository.ts:205-217` (`local_date` half-open filter), `017_time_tracking.sql:108-110` (no exclusion on clock spans). Hand-ran London BST: entry A `[2026-07-26T21:00Z, 2026-07-27T01:00Z)` and B `[2026-07-27T00:00Z, 08:00Z)` share 60 minutes; B’s week list is `local_date ∈ [2026-07-27, 2026-08-03)` so A (`local_date=2026-07-26`) is invisible. No `btree_gist` / EXCLUDE guard (only a ponytail note at `:342-346`). Not in GOLDEN-FIXES / DEFECT-LOG as this class.
**If CONFIRMED:** Reproduced via `clockOut` then a second completed write: A’s hours all roll into `week_start=2026-07-20` (240) while B rolls into `2026-07-27` (480); 60 minutes appear in both weekly totals (720 vs 660 unique). Agree S0. Correct the finder’s scenario: `createRetroactiveEntry` rejects A with `CLOCK_CROSSES_WEEK` (`:414-419`) because clock-out’s London week is `2026-07-27` ≠ clock-in’s `2026-07-20`; the hole is live on `clockOut` (no cross-week guard), then retroactive/clock-out for B.

### F-B1-2 — CONFIRMED
**Verdict basis:** Opened `timesheetCommandService.ts:511-518` (approved-week guard uses household TZ), `:546` (`timezone: shift.timezone` on create), `:1160-1172` (`rollUpIntoTimesheet` → household `weekStartOf` + `listForCarerWeek`), `017_time_tracking.sql:108-110` (`local_date` from entry `timezone`), `weekEarningsService.ts:448` (same `listForCarerWeek`). Hand-ran finder’s inputs: `2026-08-03T01:30Z` → LA `local_date=2026-08-02`, UTC `week_start=2026-08-03`; list needs `local_date >= 2026-08-03` → row excluded. Test at `timesheetCommandService.test.ts:1946-1990` only covers the approved-week guard, not roll-up inclusion. `shiftChangeRequestCommandService.ts:719-733` is arrangement `effectiveOn` dating, not the write — cite is slightly wrong but the write path is `:546`.
**If CONFIRMED:** After paid-cancel create, roll-up and earnings both sum a week window that omits the new row; `total_minutes` / earnings miss the 480 cancellation minutes while the `time_entries` row exists. Agree S0. Not already fixed/known in GOLDEN-FIXES / DEFECT-LOG.

### F-B1-3 — CONFIRMED
**Verdict basis:** Opened `timeEntryRepository.ts:174-185` / `timesheetQueryService.ts:87-99` (household week, no `carer_id`), `useWeekTimeEntries.ts:25-26`, `timesheets.ts:90-97` (`.find(t => t.week_start === weekStart)` only), `timesheetRepository.ts:99-104` (`order week_start` only — same-Monday order undefined), `ParentWeekView.tsx:245-249` / `NannyWeekView.tsx:193-194` (`sumEntryMinutes` over all entries), `weekCarerHeaderName.ts:17-18` (multi-carer name → null, totals still combined). DEFECT-LOG “Open product question” (~799) is privacy of shared entry visibility, not mismatched header vs approve target.
**If CONFIRMED:** Two carers (480 + 300) → UI `hours-total` = 780 while `getWeek` binds one arbitrary `(household, carer, week)` timesheet/earnings; approve/query act on that id. Agree S0 (wrong sheet can be approved against a combined hours view). Schema already keys timesheets by carer (`timesheets_household_carer_week_idx`).

### F-B1-4 — CONFIRMED
**Verdict basis:** Opened `timesheetCommandService.ts:280-290` (clock-in stamps household TZ), `:1160-1172` (roll-up re-derives week from current household TZ), `017_time_tracking.sql:108-120` (`local_date` frozen unless entry TZ/clock columns change), `timeEntryRepository.ts:216-217`, `householdCommandService.ts:90-97` (TZ PATCH does not rewrite entries). Hand-ran finder’s `2026-08-04T04:00Z` NY→LA: both zones yield `week_start=2026-08-03`, and `local_date=2026-08-04` stays inside `[2026-08-03, 2026-08-10)` — stated drop-to-0 does **not** follow those inputs. Corrected boundary case `2026-08-03T04:00Z`: NY `local_date=2026-08-03` / week `2026-08-03`; after LA change roll-up week is `2026-07-27` with filter `local_date < 2026-08-03` → entry excluded. D21 documents going-forward TZ edits, not this roll-up hole.
**If CONFIRMED:** Mechanism is real on a later `clockOut`/`updateEntry` roll-up after `PATCH` household timezone when old `local_date` and new `weekStartOf(clock_in)` disagree across a Monday. Agree S1. Correct wrong-number scenario to an instant whose NY local Monday and LA local Sunday fall in different Mondays (e.g. `2026-08-03T04:00:00Z`).
