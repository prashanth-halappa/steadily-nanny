### F-B4-1 — CONFIRMED
**Verdict basis:** `apps/api/src/domains/pay/services/expenseCommandService.ts:297-322` (`assertWeekNotFrozen` then `reviewPending`), `expenseCommandService.ts:517-538`, `apps/api/src/domains/pay/repositories/expenseRepository.ts:210-221` (CAS is only `expenses.status='pending'`), `timesheetCommandService.ts:807-821` / `timesheetRepository.ts:167-188` (approve CAS on timesheet), `timesheetQueryService.ts:185-190` (approved weeks use frozen `earnings`, not live recompute), `docs/11-MONEY.md:177-189`.
**If CONFIRMED:** Independently reproduced the TOCTOU: thread A passes `assertWeekNotFrozen` while the week is still `submitted`; thread B’s `approveSubmittedWithEarnings` freezes the week without the pending expense; thread A’s `reviewPending` still succeeds because it never re-checks timesheet status. `computeForWeek` / `listApprovedForWeek` only feed the frozen snapshot at approve time (`weekEarningsService.ts:446-466`), so a post-freeze approval is an approved row with no reimbursement line on the frozen statement. No DB trigger, RLS predicate, or second guard closes the window. Agree with S0.

### F-B4-2 — CONFIRMED
**Verdict basis:** `ptoCommandService.ts:245-298` (`listForHouseholdTimeOff` → `writeCorrection`), `ptoCommandService.ts:400-456` (per-day read/compute/insert loop), `ptoCommandService.ts:622-662` (`reverseNettedUsage` same pattern), `supabase/migrations/045_pto_usage_per_day.sql:34-41` (adjustments deliberately unconstrained), `ptoCommandService.ts:66-74` (unique index covers first `usage` only, not corrections).
**If CONFIRMED:** Hand-walked the finder’s correction example: net usage −480, two concurrent `markTimeOffPaid` corrections to 360 both read `paidPerDay=480`, each inserts `+120` adjustment (`minutes: -delta` at `ptoCommandService.ts:445`), net −240. Same for paired `reverseNettedUsage` calls that both read `outstanding=480` before either insert (`ptoCommandService.ts:640-652`). The comment at `ptoCommandService.ts:517-519` is scoped to cancel-vs-mark-paid reconciliation, not concurrent correction writers; it does not serialize adjustment inserts. `assertStillConfirmedAfterWrite` (`ptoCommandService.ts:521-529`) only covers cancel-during-first-mark, not two correction deltas. Agree with S0.

### F-B4-3 — CONFIRMED
**Verdict basis:** `householdCommandService.ts:136-170` (status pre-read → `createMembership` → unconditional `inviteRepo.update`), `householdMemberRepository.ts:113-132` (unique only on `(user_id, household_id)`), `supabase/migrations/009_households.sql:100-101,112-126` (no single-consumer constraint on invites), `baseRepository.ts:74-81` (unguarded update).
**If CONFIRMED:** Rebuilt trace: users A and B both `findByCode` while invite is `pending`, both pass `createMembership` (different `user_id`), both succeed; invite `accepted_by` is last-writer-wins. Repository comment at `householdMemberRepository.ts:108-111` explicitly covers same-user double-redeem only. No missed guard in middleware or migrations. Agree with S0 for unauthorized household write access (payroll-adjacent).

### F-B4-4 — REFUTED
**Verdict basis:** `timesheetCommandService.ts:807-821,864-888,1152-1251`; `timesheetRepository.ts:167-188`; `weekEarningsService.ts:271-276,446-453`; `timesheetCommandService.test.ts:2529-2557`.
**If REFUTED:** Cited lines match, but the wrong-number scenario does not hold. `computeSnapshot` calls `earnings.computeForWeek`, which reads live `time_entries` (`weekEarningsService.ts:448-453`) — not `timesheets.total_minutes` — so once B’s clock-out commits `clock_out_at`, A’s snapshot includes the new hours even if `updated_at` is still T0. If A’s CAS wins before roll-up, `rollUpIntoTimesheet` unconditionally clears the snapshot and forces `status='submitted'` (`timesheetCommandService.ts:1245-1250`), undoing a stale freeze. The repo test at `timesheetCommandService.test.ts:2529-2557` encodes the roll-up-bumped-`updated_at` CAS refusal. Permanent understating requires roll-up never running (process death), not the documented concurrent approve/clock-out race.

### F-B4-5 — REFUTED
**Verdict basis:** `timesheetCommandService.ts:902-905,960-966`; `baseRepository.ts:74-81`; `timesheetRepository.ts:185-187`; `timesheetQueryService.ts:185-190`.
**If REFUTED:** Unguarded `update` on `query`/`reopen` is real, and a query that wins after approve can leave stale `gross_minor`/`earnings` columns on a `queried` row. That does not produce a wrong paid figure: `earningsFor` recomputes live whenever `status !== 'approved'` (`timesheetQueryService.ts:185-190`), so the UI shows Estimated live earnings, not the orphaned snapshot. Totals are not double-counted. The “stuck non-approvable” outcome is a workflow state (`ACTIONABLE_STATUSES` is only `submitted` at `timesheetCommandService.ts:179`), not a silent cent error; new/edited hours re-submit via `rollUpIntoTimesheet`. Finder’s money-adjacent S1 overstates impact — would rate S2 at most.

### F-B4-6 — CONFIRMED
**Verdict basis:** `expenseCommandService.ts:165-204` (plain `expenseRepo.create`), `baseRepository.ts:53-71`, `supabase/migrations/044_expenses.sql` (no dedupe unique).
**If CONFIRMED:** Trace is accurate: two `POST` creates two `pending` rows; two parent approvals each run `reviewPending` and land in `listApprovedForWeek`, doubling `reimbursements_minor` on freeze. No idempotency key, client dedupe, or DB constraint intervenes. Disagree with S0: wrong pay requires the parent to approve both duplicates; this is duplicate-row / missing-idempotency (S1), not an silent arithmetic bug in the engine.

### F-B4-8 — CONFIRMED
**Verdict basis:** `coParentApprovalCommandService.ts:133-141`; `coParentApprovalRepository.ts:47-74`; `approvalApplierRegistry.ts:50-61`; contrast `coParentApprovalQueryService.ts:58-68` (`applyAllSettled` only on timeout sweep).
**If CONFIRMED:** `respond` CAS commits `approved`/`declined` in one statement, then `approvalApplierRegistry.apply` runs separately (`coParentApprovalCommandService.ts:139-140`); apply failure propagates after status is terminal. No outbox, retry, or transaction wraps both. Finder’s “no direct cent math” is right; stuck schedule/pay-adjacent payload is real. Agree with S1 (not S0).

### F-B4-9 — REFUTED
**Verdict basis:** `timesheetCommandService.ts:326-375`; `baseRepository.ts:74-81`; `supabase/migrations/017_time_tracking.sql:63-65`; `timesheetCommandService.ts:1143-1172`; `ClockInCard.tsx:351`; `timesheetCommandService.ts:327-328`.
**If REFUTED:** Missing `WHERE status='running'` on update is accurate, but the stated bad payroll outcome does not follow. `rollUpIntoTimesheet` full-recomputes from `listForCarerWeek` (`timesheetCommandService.ts:1166-1172`) — idempotent, no double-count (see module comment at `1143-1149`, DEFECT-LOG D6). A second sequential clock-out hits `TimeEntryNotRunningError` (`timesheetCommandService.ts:327-328`). Mobile passes `clockOut.isPending` into `ClockOutSheet` (`ClockInCard.tsx:351`), blocking typical double-tap. `time_entries_one_running_per_carer` prevents two runners, not two updates to one row; concurrent multi-device last-write-wins on `clock_out_at` is a span-metadata edge case, not duplicated week minutes. Finder’s “both roll up” does not inflate totals. S1 overstates payroll impact; would rate S2 for rare last-write-wins on session fields only.
