### F-B5-1 — CONFIRMED
**Verdict basis:** Opened `shiftChangeRequestCommandService.ts:777-790` (catch/log after accept), `timesheetCommandService.ts:487-489` / `:524-526` (`week_approved` → `TimeEntryNotEditableError`), `029_accept_shift_change_request_events.sql:118-130` (sets `cancellation_paid` under lock), `earningsService.ts:45-47` (pay only from `cancellation_paid` time entries, not the shift flag). Re-walked `respond` accept → RPC cancel → recorder; no timesheet-status check before RPC; create-time `assertMutable` (`:330`) does not consult timesheet status. Wrong-number: 480 min × 1500 / 60 = 12000 minor (£120) with no entry. Test `shiftChangeRequestCommandService.pushes.test.ts:408-426` pins swallow; `timesheetCommandService.test.ts:1928-1943` pins the throw. Not fixed in `GOLDEN-FIXES.md` / `DEFECT-LOG.md` (D1 reopens on worked roll-up; this path throws then swallows instead).
**If CONFIRMED:** Trace holds with no intervening guard or retry. Severity S0 is correct: accept returns success with `cancellation_paid = true` and no payable entry.

### F-B5-2 — CONFIRMED
**Verdict basis:** Opened `shiftCommandService.ts:138-179` (no `assertMutable`), `shiftRepository.ts:267-301` / `:76-87` (immutability only on `update`/`assertMutable`), `034_parent_shift_edit_demote_on_time_change.sql:27-52` (FOR UPDATE + time write; no `completed`/`cancelled`/time-entry check), contrast `028_open_shift_change_request_immutability.sql:43-54`. `ParentEditShiftSchema` (`schemas.ts:89-104`) only validates times/note. `freezeScheduledMinutes` (`timesheetCommandService.ts:1126-1140`) reads current shift span at clock-out. No matching fix in `GOLDEN-FIXES.md` / `DEFECT-LOG.md` (D23 only made parent edit atomic).
**If CONFIRMED:** Immutability bypass is real: parent PATCH can rewrite times while a `running` entry exists; clock-out freezes edited span (e.g. 240 vs 480 worked). Severity should be S1, not S0: worked pay still uses clock minutes; for the stated clock-out case `weekEarningsService` marks the shift `became_payable` so closure top-up does not price that shift’s shortened span as lost (`earningsService.ts:679-685`). Defect is consent/immutability + corrupted `scheduled_minutes` metadata, not the stated primary under/over-pay.

### F-B5-3 — CONFIRMED
**Verdict basis:** Opened `coParentApprovalRepository.ts:84-110` (`pending` → `timed_out`), `coParentApprovalQueryService.ts:58-71` + `approvalApplierRegistry.ts:69-86` (per-row catch/log), `scheduleHorizonJob.ts:113-124`, `coParentApprovalCommandService.ts:139-141` (explicit approve propagates applier errors), `shiftChangeRequestCommandService.ts:412-461` (`applyApprovedChangeRequest` → `assertMutable`). Test `coParentApprovalQueryService.test.ts:130-149` documents stepping over applier failures. Already-`timed_out` rows are not re-queued by `expireTimedOut`.
**If CONFIRMED:** Race reproduces: clock-in creates time entries → timeout flips `timed_out` → `assertMutable` throws → swallowed → no change request, approval stays terminal-success `timed_out`. Severity S1 stands for the stuck “silence approved” state. Correct the wrong-number: even a successful timeout only opens a change request for the carer (`:412-415`); it does not cancel or write `cancellation_paid`. The bug is orphaned `timed_out` with no ask, not direct unpaid cancel hours.

### F-B5-4 — REFUTED
**Verdict basis:** Opened `022_co_parent_approvals.sql:17-19` (`withdrawn` in CHECK), `coParentApprovalCommandService.ts:74-100` / `:113-145` (create + respond only), `householdApprovalRoutes.ts:31-44` (GET list + PATCH respond), `approval.schema.ts:10-11` / `022` header (silence auto-approves by design). Grep: no co-parent service/repo write of `withdrawn`.
**If REFUTED:** Reasoning error — missing requester `withdraw` ≠ “must wait for timeout.” Co-parent `respond` with `declined` is the cancel path (`coParentApprovalCommandService.ts:133-137`); timeout auto-apply is intentional product policy, not a broken transition. Unused CHECK enum value is schema dead weight, not an S1 payroll defect; cancel/extra appliers still leave a carer accept/decline step before money moves.
