### F-B3b-1 — CONFIRMED
**Verdict basis:** `packages/shared-types/src/schemas/schedule.schema.ts:100-103` (`UpdateSchedulePatternSchema` allows optional `status`); `apps/api/src/domains/schedule/services/schedulePatternCommandService.ts:137-146` (`update` only `assertDraft` + `patternRepo.update(patternId, input)` — no `assertCarerRole` on update, unlike `create` at `:115-116`); `apps/api/src/domains/schedule/controllers/schedulePatternController.ts:66-73` (passes `req.body`); `apps/api/src/domains/schedule/routes/schedulePatternRoutes.ts:44-48` (`authWithOwnership` + schema); `apps/api/src/domains/schedule/services/scheduleMaterialisationService.ts:191-199` (`status: 'confirmed'` on insert); `schedulePatternCommandService.ts:237-276` (`amend` requires `accepted` then `materialiseAccepted`); `schedulePatternCommandService.ts:376-387` (`materialiseForHorizon` for `listAccepted`); `supabase/migrations/014_schedule_patterns.sql:28-29` (status CHECK allows `accepted`, no transition trigger).
**If CONFIRMED:** Independent trace: draft pattern with days + `carer_id` → parent `PATCH` `{ "status": "accepted" }` while still `draft` (`assertDraft` at `:468-471` passes) → row becomes `accepted` with no `respond`/`send` and no materialisation on `update` itself → parent `POST .../amend` (`:245-276`) or horizon job (`materialiseForHorizon`) runs `materialiseAccepted` / `runMaterialisation` → confirmed shifts without carer `respond` (`:298-320` requires `pending` + assigned carer). No middleware, migration trigger, or test blocks `status: 'accepted'` on PATCH (`schedule.schema.test.ts:138-141` only exercises `withdrawn`). Severity S0 agreed: confirmed shifts are the gateway to clock-in / cancellation-paid / roll-up paths the carer never accepted.

### F-B3b-2 — CONFIRMED
**Verdict basis:** `apps/api/src/domains/household/services/householdCommandService.ts:131-170` (read invite status, `createMembership`, unconditional `inviteRepo.update` to `accepted`); `apps/api/src/domains/household/repositories/householdInviteRepository.ts:19-35` (lookup only); `apps/api/src/shared/repositories/baseRepository.ts:74-78` (unconditional update by id); `supabase/migrations/009_households.sql:100-101` + `:112-124` (unique on `(user_id, household_id)` for members, not invite single-use); no `BEFORE UPDATE` / conditional-claim trigger on `household_invites` in migrations.
**If CONFIRMED:** Hand walk with Alice and Bob: both `findByCode` while invite `pending` → both pass `:140-148` → both `createMembership` with different `user_id` (unique index does not block) → both `inviteRepo.update` to `accepted` (second overwrites `accepted_by`, both memberships remain). Sequential second redeem is blocked at `:143-144` (`householdCommandService.test.ts:377-387`); concurrent different-user redeem is not. No guard in repo or DB for single redeemer. Severity S0 agreed: two unauthorized active members on one invite code.

### F-B3b-3 — CONFIRMED
**Verdict basis:** `apps/api/src/domains/timesheet/services/timesheetQueryService.ts:71-79` (`getOwnedTimeEntry`: `carer_id === userId` only); `apps/api/src/domains/timesheet/services/timesheetCommandService.ts:250-256` (`clockIn` uses `findActiveMembership` + `CARER_ROLES`); `:326` (`clockOut` via `getOwnedTimeEntry`); `:590` (`updateEntry` via `getOwnedTimeEntry`); `apps/api/src/domains/pay/services/expenseCommandService.ts:426-439` (`loadOwnedPending`: `carer_id` only); `:228-280` (`update`/`withdraw` use `loadOwnedPending`); contrast `:449-462` (`create` uses `assertActiveNanny` → `findActiveMembership`); `apps/api/src/domains/household/repositories/householdMemberRepository.ts:27-33` (`findActiveMembership` filters `status = 'active'`); `supabase/migrations/009_households.sql:87` + `:268-271` (`removed` status + parent `UPDATE` on `household_members`).
**If CONFIRMED:** After `status = 'removed'`, `findActiveMembership` returns null → `clockIn`/`create` expense blocked; but existing entry with `carer_id = nanny` still passes `getOwnedTimeEntry` / `loadOwnedPending` → `updateEntry` can change `clock_out_at`/`break_minutes` and `rollUpIntoTimesheet` (`:655-656`) while week not `approved` (`:634-636`); pending expense `amount_minor` can be raised via `update`. No membership check on those write paths; `timeEntryRoutes.ts:30-34` ownership middleware mirrors the same `carer_id` gate. Severity S0 agreed: mutates week totals / pending reimbursements a removed carer should no longer affect.

### F-B3-1 — CONFIRMED
**Verdict basis:** `supabase/migrations/009_households.sql:268-271` + `040_rls_semantic_predicates.sql:132-135` (`UPDATE` on `household_members` with `can_write_household`, no column restriction); only `set_household_members_updated_at` trigger on that table (`009:297-298`); `apps/mobile/src/lib/supabase.ts:11-14` (anon key in client); `apps/api/src/domains/household/repositories/householdMemberRepository.ts:23-33` (API reads `role` from DB via service role); `apps/api/src/domains/timesheet/services/timesheetCommandService.ts:807-821` (`approve` + earnings freeze); `:1266-1279` (`assertWriteMember` allows `owner`/`parent` via `WRITE_ROLES` at `:174-177`); no `memberRepo.update` in `apps/api/src`.
**If CONFIRMED:** Step 1: parent JWT + anon key can PostgREST `PATCH household_members` set nanny row `role = 'parent'` (RLS permits; no trigger rejects `role` change). Step 2: nanny JWT `POST .../approve` → `getOwnedTimesheet` / `loadOwnedRow` (`timesheetQueryService.ts:221-227`) only requires active membership (any role) → `assertWriteMember` passes with escalated `parent` role → `approveSubmittedWithEarnings` freezes `gross_minor`. Nanny cannot self-escalate (nanny lacks `can_write_household` on `household_members`); escalation requires a parent write, but the defect is client-writable `role` trusted by API gates. Severity S0 agreed: carer self-approval freezes pay.

### F-B3-2 — CONFIRMED
**Verdict basis:** `supabase/migrations/015_shifts.sql:226-229` (`FOR ALL` parent write on `shifts`); `014_schedule_patterns.sql:142-152` (parent insert/update/delete on `schedule_patterns`); `040_rls_semantic_predicates.sql:274-277` (same for shifts); `apps/api/src/domains/shift/services/shiftChangeRequestCommandService.ts:336-366` (`approvalGateService.assertApprovalAllows` on short-notice cancel/time-change API path only); `apps/api/src/domains/shift/services/shiftCommandService.ts:169-179` (`applyParentEdit` RPC + events on API path); no mobile PostgREST writes to `shifts` / `schedule_patterns` / `household_members` (grep empty).
**If CONFIRMED:** Parent JWT can PostgREST `PATCH shifts` (e.g. `status: cancelled`, `cancellation_paid: true`) without hitting `approvalGateService` or `applyParentEdit`; `cancellation_paid` column has no CHECK forbidding direct set (`015_shifts.sql:53`). That does not auto-create `cancellation_paid` `time_entries` (those come from `shiftChangeRequestCommandService` → `recordCancellationPaidEntry`), so immediate wrong cents are not guaranteed — but co-parent approval and audit RPC paths are skipped and schedule state can strand (cancelled + `cancellation_paid` without change-request row / paid entry). Severity S1 appropriate: authz/audit bypass with payroll-adjacent schedule state, not immediate frozen-money S0.
