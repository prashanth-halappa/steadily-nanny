### F-B7-1 — CONFIRMED
**Verdict basis:** Opened `apps/api/src/domains/user/controllers/userController.ts:69-73`, `apps/api/src/utils/responseHelpers.ts:11-22,74-83`, `packages/shared-types/src/dto/user.dto.ts:19-21`, `apps/mobile/src/api/endpoints/user.ts:71-83,207-214`, `apps/mobile/src/hooks/mutations/useDeleteAccount.ts:18-26`, `apps/mobile/src/app/(private)/(tabs)/settings.tsx:148-159`, `apps/mobile/src/api/endpoints/__tests__/user.test.ts:276-295`, `apps/mobile/src/api/client.ts:137-139`. Not in `GOLDEN-FIXES.md` / `docs/DEFECT-LOG.md` as this delete envelope mismatch (D27–D28 are a different wire-contract class).
**If CONFIRMED:** Reproduced: `deleteUser` runs first, then `sendSuccessResponse(res, 'Account deleted', { success: true })` → envelope `{ success, message: 'Account deleted', data: { success: true } }`. Mobile parses only `response.data.data` with Zod requiring `message`; `{ success: true }` fails `safeParse` and throws. `confirmDeleteAccount` catches and returns without `signOut`, so the server-deleted account is reported as client failure and the session is kept. Line citations match. Severity S1 is fair for a destructive write that succeeds then looks failed (stuck/retry UX); not a wrong-hours/amount bug. Response interceptor does not reshape success bodies.

### F-B7-2 — CONFIRMED
**Verdict basis:** Opened `packages/shared-types/src/schemas/schedule.schema.ts:77-114`, `packages/shared-types/tests/schedule.schema.test.ts:133-141`, `apps/api/src/domains/schedule/routes/schedulePatternRoutes.ts:43-48`, `apps/api/src/domains/schedule/controllers/schedulePatternController.ts:66-73`, `apps/api/src/domains/schedule/services/schedulePatternCommandService.ts:137-147,198-221,298-320,349-397,468-471`, `apps/api/src/shared/repositories/baseRepository.ts:74-92`, `supabase/migrations/014_schedule_patterns.sql:28-30,50`, `apps/api/src/domains/availability/services/timeOffCommandService.ts:168-174` (contrast), `apps/api/src/jobs/scheduleHorizonJob.ts:66-78`, `apps/api/src/domains/schedule/repositories/schedulePatternRepository.ts:43-56`. Grep: `useUpdateSchedulePattern` has no UI callers. Not documented in `GOLDEN-FIXES.md` / `DEFECT-LOG.md`.
**If CONFIRMED:** Reproduced: on a **draft**, parent `PATCH` body `{ "status": "accepted" }` passes `UpdateSchedulePatternSchema`, `assertDraft` only checks pre-update status, then `patternRepo.update(patternId, input)` writes `status` with no strip, no `responded_at`, no carer check, no `materialiseAccepted` (unlike `respond` at `:311-319`). DB CHECK allows `'accepted'`; no trigger ties accept to materialisation. Line citations match. Corrected scenario: this is a **consent / state-machine bypass** (force-accept without `send`/`respond`), not a lasting “no shifts → wrong pay” path — `listAccepted` + daily `materialiseForHorizon` can still create shifts later; first-party mobile never calls update with `status` (hook unused). Propose **S2** (API hole / consent integrity), not S1 payroll wrong-number.
