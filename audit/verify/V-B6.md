### F-B6-1 — CONFIRMED
**Verdict basis:** `reminderLogRepository.ts:24-47` (`claim` INSERTs unconditionally); `047_push_reminder_log.sql:8-11,16-26` (PK `(user_id, reminder_key)`, comment “INSERTs BEFORE sending”); `reminderJob.ts:380-397,449-470,522-538` (`claim` then `await deps.push.notifyUser`); `reminderJob.ts:320-326` (`defaultPushService.notifyUser` calls fire-and-forget `householdPush.notifyUser`); `householdPush.ts:39-46` (`void sendToUser`, errors swallowed); `pushDispatchService.ts:28-36` (`shouldDeliverPush` gate + zero-token early return); `notificationPrefsService.ts:118-203` (`shouldDeliverPush`); `constants.ts:17-27` (`TIMESHEET_AWAITING_APPROVAL` and `SHIFT_REMINDER` not in `QUIET_HOURS_EXEMPT_TYPES`); `quietHours.ts:32-64` (`[start, end)` exclusive end — 09:00 is inside 22:00–10:00).
**If CONFIRMED:** Re-walked `POST /api/jobs/reminders` → `runReminderJob` → `processTimesheetReminders`: at local 09:00, `clock.hour === 9` passes (`reminderJob.ts:444-447`), `claim` inserts (`453-457`), then `notifyUser` → `sendToUser` → `shouldDeliverPush` returns false during quiet hours (`pushDispatchService.ts:28-30`) — no Expo send, but the PK row remains. Same ordering for shift reminders (`381-397`). `stats.sent++` increments after `notifyUser` even though `householdPush.notifyUser` does not await delivery (`reminderJob.ts:397,470`; `householdPush.ts:40-41`). For timesheet keys, `buildTimesheetAwaitingApprovalKey` embeds `clock.date` (`reminderJob.ts:177-187`), so suppression repeats daily at 09:00 while quiet hours still cover 09:00 — not a one-day miss. For shift reminders, `buildShiftReminderKey` is once-per-shift (`173-175`) plus hour===18 gate (`361-364`) — a blocked 18:00 claim is permanent. Agree with S1: no wrong cent by itself, but silent loss of approval nudges on the hours/pay path. Not in `GOLDEN-FIXES.md` or `docs/DEFECT-LOG.md` (047/048 not applied in prod per `A3-prod-ground-truth.md`, but the code path is live in repo).

### F-B6-3 — CONFIRMED
**Verdict basis:** `scheduleHorizonJob.ts:66-88` → `materialiseForHorizon`; `schedulePatternCommandService.ts:399-442` (`expandRecurrence` from `pattern.dtstart` through `addDays(now, horizonDays)`); `recurrenceExpander.ts:245-263` (loop from `dtstartEpoch`); `scheduleMaterialisationService.ts:190-207` (missing row → `create({ status: 'confirmed', … })` with no `startsAt` vs `now` check); `scheduleMaterialisationService.ts:261-264` (`isFuture` only on orphan **delete** path, not create).
**If CONFIRMED:** Hand trace: pattern accepted day 0 materialised through day 84; horizon job absent days 85–89; on day 90 `expandRecurrence` still emits occurrences for days 85–89; `findByPatternAndDate` returns null; `materialiseOne` creates past `confirmed` shifts (`190-206`). Auto-match cannot attach: `matchConfirmedShift` only matches shifts within ±2h of `clockInAt` (`timesheetCommandService.ts:1057-1078`). Pay/hours misattribution via explicit `shift_id` is reproducable: `clockIn` with `input.shift_id` calls `assertShiftBelongsToCarer` with no temporal guard (`265-271`, `1020-1032`); shipping mobile clock-in sends only `household_id` (`ClockInCard.tsx:170`), so the misattribution path is API/retroactive (`createRetroactiveEntry` `441-448`) not the default Today flow — but the API path is unguarded. Phantom shifts alone pin schedule immutability once any `time_entry` links (`scheduleMaterialisationService.ts:213-214`). Agree with S1 for schedule/payroll integrity; not a direct wrong-cent bug without a `shift_id` write. Not documented in `GOLDEN-FIXES.md`; `DEFECT-LOG.md` D12 covers cross-household `shift_id` (fixed), not past backfill.
