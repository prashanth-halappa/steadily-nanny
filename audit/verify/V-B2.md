### F-B2-1 — CONFIRMED
**Verdict basis:** `timesheetCommandService.ts:546` (`timezone: shift.timezone`), `:511-518` + `:1161-1172` (household `weekStartOf` then `listForCarerWeek`), `timeEntryRepository.ts:216-217` (`local_date` range filter), `017_time_tracking.sql:108-110` (trigger uses entry `timezone`), `015_shifts.sql` shift `timezone`/`local_date`.
**If CONFIRMED:** With household `Pacific/Auckland` and entry `timezone: Europe/London`, `starts_at=2026-08-02T14:00:00Z` → trigger `local_date=2026-08-02`; `weekStartOf(instant, Auckland)=2026-08-03`; `listForCarerWeek(..., '2026-08-03', '2026-08-10')` excludes `local_date=2026-08-02`, so `rollUpIntoTimesheet`/`sumWorkedMinutes` and `weekEarningsService.computeForWeek` (`weekEarningsService.ts:448-453`) never count the 480 cancellation minutes. No guard realigns `timezone` to household (contrast clock-in at `:290`). Divergence is reachable when `shifts.timezone` is stale vs current `household.timezone` (`ManageHouseholdScreen.tsx` documents frozen shift zones). Agree with S0 when zones disagree.

### F-B2-2 — REFUTED
**Verdict basis:** `weekEarningsService.ts:145-151`, `:271-286`, `:317-321`; `earningsService.ts:450-451`, `:605-612`, `:747-758`; `timesheetCommandService.ts:245-293`; `docs/11-MONEY.md` §5/§7; `earningsService.test.ts:772-788`.
**If REFUTED:** The cited netting (`weekEarningsService.ts:145-151`) fixes **cancelled-then-worked** (usage + reversing `adjustment` on the same `time_off_id`), documented in `docs/11-MONEY.md:276-278`. It does not claim mutual exclusion for confirmed time off + worked hours. Product rule: `payable_minutes = worked + cancellation_paid + PTO usage` (`docs/11-MONEY.md:329-332`, `TIER0-PLAN.md:491`). `computeWeekEarnings` independently sums `workedByDate` and `ptoByDate` (`earningsService.ts:450-451`, `:605-612`); `earningsService.test.ts:772-788` asserts additive gross (`29600` for 480+480). Same-day Mon PTO 480 + Mon worked 480 → `pto` `14800` + `regular` `14800` = `29600` by spec, not omission. Finder’s “expected one line” is not a product invariant.

### F-B2-3 — REFUTED
**Verdict basis:** `ptoCommandService.ts:234-300`; `expenseCommandService.ts:297-299`, `:517-538`; `timesheetQueryService.ts:185-198`; `docs/11-MONEY.md` §3.
**If REFUTED:** Asymmetric freeze is documented: §3 mandates `assertWeekNotFrozen` for **expense approval only** (`docs/11-MONEY.md:177-189`, `expenseCommandService.ts:517-538`), because approving adds reimbursement money; rejecting stays allowed. Approved timesheets **intentionally** return frozen `earnings` (`timesheetQueryService.ts:185-195`) and never recompute. `markTimeOffPaid` appends ledger rows (`ptoCommandService.ts:234-300`) — balance/ledger state, not the frozen wage snapshot. Post-approve PTO appearing on the statement requires `POST /timesheets/:id/reopen` (`docs/11-MONEY.md:155-158`), same class as post-approve hour changes (D1). “Invisible £14800” assumes approved gross should auto-include post-approve PTO without reopen; that contradicts §3. Not S0 automatic wrong payment if disbursement follows the frozen statement.

### F-B2-4 — CONFIRMED
**Verdict basis:** `timesheetCommandService.ts:245-293` (clock-in, no overlap check), `:350-357` + `:732-762` (`assertNoOverlap` on clock-out), `:495-547` (full-span `cancellation_paid`); `017_time_tracking.sql:63-65` (`time_entries_one_running_per_carer`); `:582` (running entries not editable).
**If CONFIRMED:** `matchConfirmedShift` (`:1073-1077`) skips `cancelled` shifts, so adhoc clock-in during a paid-cancel window succeeds. Cancel entry 09:00–17:00 + clock-in 10:00 → any `clock_out > clock_in` overlaps (`inMs < otherOut && otherIn < outMs` at `:753`). `TimeEntryOverlapError` on every clock-out; `findRunningForCarer` + unique index block further clock-ins. No API path deletes/abandons the running row. Agree S1 (operational deadlock, not cent miscalculation).

### F-B2-5 — CONFIRMED
**Verdict basis:** `shiftChangeRequestCommandService.ts:777-790`; `timesheetCommandService.ts:524-526`; `039_cancellation_paid_unique.sql`; `shiftChangeRequestCommandService.pushes.test.ts:408-427`.
**If CONFIRMED:** RPC sets `cancellation_paid=true` before recorder; `catch` logs and returns success (`:781-790`, comment “never fail the accept”). `recordCancellationPaidEntry` throws on approved week (`timesheetCommandService.ts:524-526`) — swallowed. Test asserts accept succeeds when recorder throws. Shift shows paid cancel; timesheet/earnings `0` for those minutes. Agree S0 for silent underpay on recorder failure; severity fair given money path.
