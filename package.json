{
  "name": "monorepo-template",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "dev:mobile": "turbo run dev --filter=@steadily-nanny/mobile",
    "dev:api": "turbo run dev --filter=@steadily-nanny/api",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "biome check --write --unsafe . && biome format --write .",
    "format:check": "biome format .",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean && rm -rf node_modules",
    "qc": "./scripts/qc.sh",
    "setup": "bun scripts/setup.ts",
    "g": "sh -c 'git add . && git status && git commit -m \"$1\" && git push' _",
    "prepare": "husky"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.0",
    "husky": "9.1.7",
    "lint-staged": "16.2.7",
    "turbo": "2.9.18",
    "typescript": "5.9.3"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "overrides": {
    "zod": "4.4.3"
  },
  "packageManager": "bun@1.3.9",
  "// TODO(porting)": "Source repo patches expo-constants, expo-updates, and @sentry/react-native via patchedDependencies + patches/*.patch (workarounds for specific Expo/Sentry bugs at specific versions). Not carried over here — re-evaluate once apps/mobile's real dependency versions are extracted, and only reintroduce patchedDependencies if the same bugs reproduce against this template's pinned versions.",
  "lint-staged": {
    "//": "Scoped to code dirs on purpose: docs/templates/biome.json is a copy-paste teaching file that Biome 2.x treats as a conflicting nested ROOT config if it (or any file near it) is ever passed explicitly. docs/** is biome-ignored anyway.",
    "{apps,packages,scripts}/**/*.{ts,tsx}": [
      "biome check --write --no-errors-on-unmatched",
      "biome format --write --no-errors-on-unmatched"
    ],
    "{apps,packages,scripts}/**/*.json": [
      "biome format --write --no-errors-on-unmatched"
    ],
    "./*.{json,ts}": [
      "biome format --write --no-errors-on-unmatched"
    ]
  }
}
