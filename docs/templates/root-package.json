{
  "name": "yourapp",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "api": "cd apps/api && bun run dev",
    "mobile": "cd apps/mobile && bun run dev",
    "web": "cd apps/web && bun run dev",
    "dev:mobile": "turbo run dev --filter=@yourapp/mobile",
    "dev:api": "turbo run dev --filter=@yourapp/api",
    "dev:web": "turbo run dev --filter=@yourapp/web",
    "build": "turbo run build",
    "build:web": "turbo run build --filter=@yourapp/web",
    "lint": "turbo run lint",
    "format": "biome check --write --unsafe . && biome format --write .",
    "format:check": "biome format .",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean && rm -rf node_modules",
    "qc": "./scripts/qc.sh",
    "g": "sh -c 'git add . && git status && git commit -m \"$1\" && git push' _",
    "prepare": "husky"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.10",
    "husky": "9.1.7",
    "lint-staged": "16.2.7",
    "playwright": "1.58.2",
    "turbo": "2.7.2",
    "typescript": "5.9.3"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "overrides": {
    "zod": "4.3.6"
  },
  "packageManager": "bun@1.3.9",
  "lint-staged": {
    "*.{ts,tsx}": [
      "biome check --write --no-errors-on-unmatched",
      "biome format --write --no-errors-on-unmatched"
    ],
    "*.json": [
      "biome format --write --no-errors-on-unmatched"
    ]
  }
}
