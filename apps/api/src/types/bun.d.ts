// Pull in Bun's global type declarations and the `bun:test` module types.
// bun-types is a plain package (not an @types/* package), so it must be
// referenced explicitly rather than picked up via typeRoots auto-inclusion.
/// <reference types="bun-types" />
