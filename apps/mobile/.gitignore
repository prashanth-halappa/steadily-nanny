# Expo / RN
.expo/
dist/
web-build/
ios/
android/
*.orig.*

# Native secrets — NEVER commit
.env
.env.*
!.env.example
!.env.maestro.example
google-services.json
GoogleService-Info.plist
*.keystore
*.p8
*.p12
*.mobileprovision
SETUP-play-service-account.json

# Logs / caches
logs/
*.log
node_modules/
.turbo/
coverage/

# Generated CSS from shell redirect slip (e.g., --output -)
-

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli
# Maestro screenshot output — regenerable by running the walkthrough,
# ~10MB of binaries that would otherwise live in git forever.
.maestro/screenshots/
