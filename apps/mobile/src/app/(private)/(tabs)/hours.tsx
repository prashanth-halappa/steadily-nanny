/**
 * @module app/(private)/(tabs)/hours
 * Clock in/out history + weekly approval. See
 * `src/domains/timesheet/components/HoursScreen` for the real implementation.
 */
import { HoursScreen } from '@/src/domains/timesheet';

export default function HoursRoute() {
  return <HoursScreen />;
}
