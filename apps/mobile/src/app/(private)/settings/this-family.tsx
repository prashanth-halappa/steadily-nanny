/**
 * @module app/(private)/settings/this-family
 *
 * Route: `/settings/this-family` (nanny/helper only). See
 * `src/domains/household/components/ThisFamilyScreen` for the
 * implementation.
 */
import { ThisFamilyScreen } from '@/src/domains/household/components/ThisFamilyScreen';

export default function ThisFamilyRoute() {
  return <ThisFamilyScreen />;
}
