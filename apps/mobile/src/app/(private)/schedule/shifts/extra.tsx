/**
 * One-off extra shift form — parent Schedule calendar entry point.
 */
import { ExtraShiftScreen } from '@/src/domains/schedule/components/ExtraShiftScreen';

export default function ExtraShiftRoute() {
  return <ExtraShiftScreen />;
}
