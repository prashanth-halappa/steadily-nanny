/**
 * @module domains/household
 * Public surface of the household domain — Wave B's multi-household
 * switcher. Route files / other domains import from here rather than
 * reaching into `components/` directly.
 */
export { HouseholdSwitcher } from './components/HouseholdSwitcher';
