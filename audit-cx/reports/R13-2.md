### F-CX-R13-2-1 -- Profile rows are hand-rolled with borders instead of grouped in a borderless Card
**Severity:** S1
**Where:** apps/mobile/src/domains/household/components/CarerProfileScreen.tsx:73
**Evidence:** `      className="flex-row items-center justify-between rounded-row border border-border bg-background px-4 py-3"`
**Rule:** 01-LAWS.md Rule D and 00-FOUNDATIONS.md §5.2. L3-list rows must live inside ONE `Card tone="default" p-0 overflow-hidden`, and surfaces carry no border.
**What the user sees:** The list of profile rows looks like separated, bordered blocks instead of a cohesive grouped card separated by light.
**Fix:** Wrap the profile rows in a single `<Card tone="default" p-0>` and remove the per-row border classes in CarerProfileScreen.tsx.

### F-CX-R13-2-2 -- Destructive list action uses a bordered row instead of a ghost button
**Severity:** S1
**Where:** apps/mobile/src/domains/household/components/CarerProfileScreen.tsx:239
**Evidence:** `        className="mt-6 rounded-row border border-border bg-background px-4 py-3"`
**Rule:** screens-settings.md §3 and 00-FOUNDATIONS.md §5.2. Destructive actions at the bottom of settings screens should use a ghost button with destructive text, and hand-rolled borders are banned.
**What the user sees:** The "Remove from this household" action renders as a full bordered row instead of the system's standard quiet destructive button treatment.
**Fix:** Replace the bordered pressable with a `<Button variant="ghost">` carrying `text-destructive` in CarerProfileScreen.tsx.

### F-CX-R13-2-3 -- Single row uses a hand-rolled bordered surface instead of a Card
**Severity:** S1
**Where:** apps/mobile/src/domains/household/components/ThisFamilyScreen.tsx:210
**Evidence:** `        className="mt-6 flex-row items-center justify-between rounded-row border border-border bg-background px-4 py-3"`
**Rule:** 00-FOUNDATIONS.md §5.2. Surfaces are separated by shadow, never a hairline rule. Use `<Card>` instead of hand-rolling a view with `border border-border`.
**What the user sees:** The Terms row appears with a hairline border, clashing with the borderless shadow elevation of the cards around it.
**Fix:** Replace the hand-rolled bordered pressable with a standard `<Card>` in ThisFamilyScreen.tsx.

### F-CX-R13-2-4 -- Section headers use the L1 card title heading tier
**Severity:** S1
**Where:** apps/mobile/src/domains/household/components/ThisFamilyScreen.tsx:117
apps/mobile/src/domains/household/components/ThisFamilyScreen.tsx:174
apps/mobile/src/domains/household/components/ThisFamilyScreen.tsx:216
**Evidence:** `        <H3>{t('thisFamily.ifSomethingHappens')}</H3>`
**Rule:** 01-LAWS.md Rule A. Section headers must render as `DayGroup` or `H2` at top level, never `H3` which is reserved for L1 cards.
**What the user sees:** The section headers carry the incorrect typographic weight, disrupting the screen's visual hierarchy.
**Fix:** Change the `<H3>` components to `<H2>` for the section headers in ThisFamilyScreen.tsx.

### F-CX-R13-2-5 -- Incorrect top spacing for section headers
**Severity:** S3
**Where:** apps/mobile/src/domains/household/components/ThisFamilyScreen.tsx:116
apps/mobile/src/domains/household/components/ThisFamilyScreen.tsx:173
apps/mobile/src/domains/household/components/ThisFamilyScreen.tsx:215
**Evidence:** `      <View testID="this-family-if-something-happens" className="mt-6 gap-2">`
**Rule:** 01-LAWS.md Rule B. Space above a group header must be 32px (`pt-8` or `mt-8`), which is ~4x the space below it.
**What the user sees:** The sections are placed too close to the content above them, breaking the rhythm of the layout.
**Fix:** Change `mt-6` to `mt-8` on the section wrapper views in ThisFamilyScreen.tsx.

CLEAN: apps/mobile/src/domains/setup/components/ManageInviteScreen.tsx, apps/mobile/src/domains/setup/components/InviteScreen.tsx
