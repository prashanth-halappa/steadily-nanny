### F-CX-R9b-3-1 -- L1 card title uses wrong hierarchy rung
**Severity:** S1
**Where:** apps/mobile/src/domains/pay/components/ScheduledChangeCard.tsx:56
**Evidence:** `<H4>`
**Rule:** 01-LAWS.md 1, the rung model dictates that an L1 attention card must use an H3 title.
**What the user sees:** The title of the scheduled change card lacks the correct typographic weight and size for the most important item on the screen.
**Fix:** Change H4 to H3 in the component layer of ScheduledChangeCard.tsx.

### F-CX-R9b-3-2 -- Text color fails contrast on tinted ground
**Severity:** S1
**Where:** apps/mobile/src/domains/pay/components/ScheduledChangeCard.tsx:61
**Evidence:** `<Small testID="pay-scheduled-diff" className="text-muted-foreground">`
**Rule:** 01-LAWS.md 4, Rule M states that mutedForeground fails AA contrast on tinted grounds and must be mutedStrong.
**What the user sees:** The summary of the scheduled changes is hard to read against the tinted card background.
**Fix:** Change text-muted-foreground to text-muted-strong and Small to Body in the component layer of ScheduledChangeCard.tsx.

### F-CX-R9b-3-3 -- PayChangeSheet renders as a bottom sheet instead of a full screen
**Severity:** S1
**Where:** apps/mobile/src/domains/pay/components/PayChangeSheet.tsx:283
**Evidence:** `<BottomSheetBase`
**Rule:** screens-pay-terms.md 7.1, PayChangeSheet becomes a full screen, not a sheet, to support a diff-first review.
**What the user sees:** The terms change flow renders as a modal bottom sheet, preventing the user from using the correct diff-first flow.
**Fix:** Refactor the component to use a screen shell instead of a BottomSheetBase in PayChangeSheet.tsx.

### F-CX-R9b-3-4 -- Navigation affordance uses button instead of text link
**Severity:** S1
**Where:** apps/mobile/src/domains/pay/components/PaySetupScreen.tsx:365
**Evidence:** `                  variant="ghost"`
**Rule:** 01-LAWS.md 5.G, the affordance grammar specifies that navigating away to read more must use a text-primary link, not a button variant.
**What the user sees:** The user sees a button styling for an action that merely navigates to another view without mutating state.
**Fix:** Change the button variant to a link or replace it with a text-primary link in PaySetupScreen.tsx.

CLEAN: apps/mobile/src/domains/pay/components/PaySetupPromptCard.tsx, apps/mobile/src/app/(private)/settings/pay/index.tsx, apps/mobile/src/app/(private)/settings/my-pay.tsx, apps/mobile/src/app/(private)/settings/pay/[carerId].tsx, apps/mobile/src/app/(private)/settings/pay/setup/[carerId].tsx
