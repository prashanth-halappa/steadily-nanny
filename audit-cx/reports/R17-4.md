### F-CX-R17-4-1 -- SignatureHeroLight decreases weight at the top of the importance ramp
**Severity:** S1
**Where:** apps/mobile/src/components/ui/typography/signature.tsx:10
**Evidence:** `export const SignatureHeroLight = createTypographyComponent(`
**Rule:** 01-LAWS.md section 7, weight must never decrease as a token moves up the importance ramp. 
**What the user sees:** A top-tier "Hero" anchor figure renders in a lighter weight than the routine headings (like H1) below it, breaking the screen's visual hierarchy.
**Fix:** Remove `SignatureHeroLight` from `apps/mobile/src/components/ui/typography/signature.tsx` (and its token), or re-weight it to at least 700 to match the rest of the Hero tier.

### F-CX-R17-4-2 -- FieldError uses the base destructive hue for text instead of the deep error ink
**Severity:** S1
**Where:** apps/mobile/src/components/ui/field-error.tsx:17
**Evidence:** `      className={cn('mt-2 text-destructive', className)}`
**Rule:** 00-FOUNDATIONS.md section 3.2, `errorInlineText` is the dedicated destructive pill/label ink and no second destructive ink token should be added.
**What the user sees:** The validation message renders in the lighter base destructive hue rather than the deep error ink, reducing legibility against the background.
**Fix:** Change `text-destructive` to `text-error-inline-text` in `apps/mobile/src/components/ui/field-error.tsx`.

### F-CX-R17-4-3 -- LiveDot is missing its required pulse animation
**Severity:** S1
**Where:** apps/mobile/src/components/ui/live-dot.tsx:12
**Evidence:** `    <View`
**Rule:** 00-FOUNDATIONS.md section 10, the LiveDot must carry the existing pulse animation and remain unchanged.
**What the user sees:** The apricot live indicator is a static dot instead of a pulsing signal, removing the motion that indicates an active, ongoing state.
**Fix:** Change the primitive in `apps/mobile/src/components/ui/live-dot.tsx` to a Reanimated `Animated.View` (styled via inline style rather than className) and restore the pulse.

CLEAN: apps/mobile/src/components/ui/typography/utility.tsx, apps/mobile/src/components/ui/typography/types.ts, apps/mobile/src/components/ui/field-label.tsx, apps/mobile/src/components/ui/typography.tsx
