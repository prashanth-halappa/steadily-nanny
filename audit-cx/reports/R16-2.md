### F-CX-R16-2-1 -- Screen header read-only badge uses a banned border
**Severity:** S1
**Where:** apps/mobile/src/components/ui/screen-header.tsx:110
**Evidence:** `              className="mt-1 flex-row items-center gap-1 self-start rounded-chip border border-border bg-card px-3 py-1"`
**Rule:** 01-LAWS.md section 6, the ban on card borders and hairlines stands, with only specific sanctioned exceptions like D's inset hairline and form fields; chips are not an exception.
**What the user sees:** A 1px border around the read-only chip, contradicting the shadow-based elevation system and the system border ban.
**Fix:** Remove the border classes in `screen-header.tsx` and use a tinted pill background for the component.

### F-CX-R16-2-2 -- Screen header exceeds the three-element cap when readOnly is present
**Severity:** S1
**Where:** apps/mobile/src/components/ui/screen-header.tsx:107
**Evidence:** `          {readOnly ? (`
**Rule:** 01-LAWS.md section 5.C, the hero band is capped at exactly three elements: an H1, one context line, and one anchor.
**What the user sees:** A cluttered header band stacked with four elements when a household is read-only, breaking the vertical rhythm.
**Fix:** Consolidate the read-only state into the context line within `screen-header.tsx` to maintain the three-element cap.

### F-CX-R16-2-3 -- Time range picker labels use arbitrary text sizes below the system minimum
**Severity:** S1
**Where:** apps/mobile/src/components/ui/time-range-picker.tsx:74
apps/mobile/src/components/ui/time-range-picker.tsx:86
**Evidence:** `          <Text className="mb-1 font-medium text-muted-foreground text-xs">`
**Rule:** 00-FOUNDATIONS.md section 1, the typography minimum is 16px for body, and the scale is semantic; arbitrary sizing values must not be used.
**What the user sees:** Field labels that are smaller than the app's baseline readability floor.
**Fix:** Replace the hardcoded `text-xs` utility with a standard semantic typography component in `time-range-picker.tsx`.

### F-CX-R16-2-4 -- Time range picker error text uses an arbitrary text size below the system minimum
**Severity:** S1
**Where:** apps/mobile/src/components/ui/time-range-picker.tsx:102
**Evidence:** `          className="mt-2 text-destructive text-xs"`
**Rule:** 00-FOUNDATIONS.md section 1, the typography minimum is 16px for body, and the scale is semantic; arbitrary sizing values must not be used.
**What the user sees:** Error messages that are smaller than the app's baseline readability floor.
**Fix:** Replace the hardcoded `text-xs` utility with a semantic typography component in `time-range-picker.tsx`.

### F-CX-R16-2-5 -- Icon button size fails the minimum touch target requirement on native
**Severity:** S1
**Where:** apps/mobile/src/components/ui/button.tsx:31
**Evidence:** `        icon: 'h-10 w-10',`
**Rule:** 00-FOUNDATIONS.md section 1, spacing is an 8pt grid where 44px is the rigid platform minimum touch target.
**What the user sees:** A tap target that is only 40px on mobile devices, leading to missed taps and frustration.
**Fix:** Add `native:h-12 native:w-12` to the icon size variant in `button.tsx` to clear the 44px floor.

### F-CX-R16-2-6 -- Switch height fails the minimum touch target requirement
**Severity:** S1
**Where:** apps/mobile/src/components/ui/switch.tsx:68
**Evidence:** `          height: 32,`
**Rule:** 00-FOUNDATIONS.md section 1, spacing is an 8pt grid where 44px is the rigid platform minimum touch target.
**What the user sees:** A control whose tap target is only 32px tall on mobile, leading to missed taps.
**Fix:** Introduce a `hitSlop` or wrap the native Switch in a 44px container in `switch.tsx`.

CLEAN: apps/mobile/src/components/ui/list-group.tsx, apps/mobile/src/components/ui/section.tsx, apps/mobile/src/components/ui/date-time-field.tsx, apps/mobile/src/components/ui/textarea.tsx
