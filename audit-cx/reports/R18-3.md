### F-CX-R18-3-1 -- `mutedForeground` used on tinted live card instead of `mutedStrong`
**Severity:** S1
**Where:** apps/mobile/src/widgets/TodaysCoverWidget.tsx:352
**Evidence:** `          foregroundStyle(MUTED),`
**Rule:** 01-LAWS.md Section 4, "any Small / Caption / MetadataLabel that sits on a wash, on surfaceAttention, surfacePositive, surfaceCritical or surfaceLive uses text-muted-strong, not text-muted-foreground."
**What the user sees:** Secondary text on the live card has insufficient contrast, making it difficult to read against the apricot-tinted ground.
**Fix:** In apps/mobile/src/widgets/TodaysCoverWidget.tsx, conditionally use the `mutedStrong` hex for text color when the widget is in the `live` state.

### F-CX-R18-3-2 -- `mutedForeground` used on tinted live card instead of `mutedStrong`
**Severity:** S1
**Where:** apps/mobile/src/widgets/TodaysCoverWidget.tsx:384
**Evidence:** `            foregroundStyle(MUTED),`
**Rule:** 01-LAWS.md Section 4, "any Small / Caption / MetadataLabel that sits on a wash, on surfaceAttention, surfacePositive, surfaceCritical or surfaceLive uses text-muted-strong, not text-muted-foreground."
**What the user sees:** Secondary text on the live card has insufficient contrast, making it difficult to read against the apricot-tinted ground.
**Fix:** In apps/mobile/src/widgets/TodaysCoverWidget.tsx, conditionally use the `mutedStrong` hex for text color when the widget is in the `live` state.

### F-CX-R18-3-3 -- `mutedForeground` used on tinted live card instead of `mutedStrong`
**Severity:** S1
**Where:** apps/mobile/src/widgets/TodaysCoverWidget.tsx:416
**Evidence:** `      modifiers={[font({ size: 11 }), foregroundStyle(MUTED), lineLimit(1)]}`
**Rule:** 01-LAWS.md Section 4, "any Small / Caption / MetadataLabel that sits on a wash, on surfaceAttention, surfacePositive, surfaceCritical or surfaceLive uses text-muted-strong, not text-muted-foreground."
**What the user sees:** Secondary text on the live card has insufficient contrast, making it difficult to read against the apricot-tinted ground.
**Fix:** In apps/mobile/src/widgets/TodaysCoverWidget.tsx, conditionally use the `mutedStrong` hex for text color when the widget is in the `live` state.

### F-CX-R18-3-4 -- `mutedForeground` used on tinted live card instead of `mutedStrong`
**Severity:** S1
**Where:** apps/mobile/src/widgets/TodaysCoverWidget.tsx:436
**Evidence:** `          modifiers={[font({ size: 11 }), foregroundStyle(MUTED), lineLimit(1)]}`
**Rule:** 01-LAWS.md Section 4, "any Small / Caption / MetadataLabel that sits on a wash, on surfaceAttention, surfacePositive, surfaceCritical or surfaceLive uses text-muted-strong, not text-muted-foreground."
**What the user sees:** Secondary text on the live card has insufficient contrast, making it difficult to read against the apricot-tinted ground.
**Fix:** In apps/mobile/src/widgets/TodaysCoverWidget.tsx, conditionally use the `mutedStrong` hex for text color when the widget is in the `live` state.

### F-CX-R18-3-5 -- `mutedForeground` used on tinted live card instead of `mutedStrong`
**Severity:** S1
**Where:** apps/mobile/src/widgets/TodaysCoverWidget.tsx:497
**Evidence:** `                      foregroundStyle(MUTED),`
**Rule:** 01-LAWS.md Section 4, "any Small / Caption / MetadataLabel that sits on a wash, on surfaceAttention, surfacePositive, surfaceCritical or surfaceLive uses text-muted-strong, not text-muted-foreground."
**What the user sees:** Secondary text on the live card has insufficient contrast, making it difficult to read against the apricot-tinted ground.
**Fix:** In apps/mobile/src/widgets/TodaysCoverWidget.tsx, conditionally use the `mutedStrong` hex for text color when the widget is in the `live` state.

### F-CX-R18-3-6 -- Semantic hue used for primary sentence text
**Severity:** S1
**Where:** apps/mobile/src/widgets/TodaysCoverWidget.tsx:373
**Evidence:** `          foregroundStyle(lead && lead.kind === 'gap' ? OCHRE : FG),`
**Rule:** 01-LAWS.md Section 3, "The primary sentence — the headline, the main statement the tier exists to deliver — is foreground."
**What the user sees:** The main statement for a gap is written in ochre instead of the foreground color, making it harder to read and undercutting the tier's hierarchy.
**Fix:** In apps/mobile/src/widgets/TodaysCoverWidget.tsx, change the foregroundStyle to always use the FG color for the primary sentence text.

### F-CX-R18-3-7 -- `StatusPill` equivalent uses an alpha fill
**Severity:** S1
**Where:** apps/mobile/src/widgets/NannyWeekWidget.tsx:187
**Evidence:** `  const pillBg = \`${statusFg}29\`;`
**Rule:** 00-FOUNDATIONS.md Section 8.2, "Filled, opaque fills with deep inks — never an alpha fill"
**What the user sees:** The status pill has a translucent background instead of a solid opaque one, which mixes with the background color and reduces text contrast.
**Fix:** In apps/mobile/src/widgets/NannyWeekWidget.tsx, replace the computed alpha `pillBg` with the exact opaque hex values defined for pill variants.

### F-CX-R18-3-8 -- `StatusPill` equivalent uses semantic hue for text instead of deep ink token
**Severity:** S1
**Where:** apps/mobile/src/widgets/NannyWeekWidget.tsx:196
**Evidence:** `          foregroundStyle(statusFg),`
**Rule:** 00-FOUNDATIONS.md Section 8.2, which dictates `text-success-ink` and `text-short-notice-ink` for pill text colors, never the base semantic hues.
**What the user sees:** The pill text uses a lighter semantic hue instead of the high-contrast deep ink, leading to poor legibility and contrast against the fill.
**Fix:** In apps/mobile/src/widgets/NannyWeekWidget.tsx, replace `statusFg` in the foregroundStyle with the appropriate deep ink hex color.

### F-CX-R18-3-9 -- Rounded font design used instead of the single embedded family
**Severity:** S2
**Where:** apps/mobile/src/widgets/NannyWeekWidget.tsx:286
**Evidence:** `          design: 'rounded',`
**Rule:** 00-FOUNDATIONS.md Section 11, "No new font, no per-component fontFamily."
**What the user sees:** The hero hours figure is rendered in a rounded system font, breaking visual consistency with the app's single typographic style.
**Fix:** In apps/mobile/src/widgets/NannyWeekWidget.tsx, remove `design: 'rounded'` from the font modifiers so the widget uses the standard font.

CLEAN:
