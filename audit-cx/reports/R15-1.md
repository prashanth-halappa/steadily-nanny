### F-CX-R15-1-1 -- L1 card body text uses mutedForeground instead of mutedStrong
**Severity:** S1
**Where:** apps/mobile/src/domains/inbox/components/PendingOfferCard.tsx:163
**Evidence:** `        className="text-muted-foreground"`
**Rule:** 01-LAWS.md §1 and §4, The rung model L1 requires body text to be mutedStrong, and Rule M requires mutedStrong for secondary text on tinted grounds.
**What the user sees:** The subtitle text lacks legibility and contrast when the card turns into a tinted attention state.
**Fix:** Conditionally apply text-muted-strong when the card is blocking in PendingOfferCard.tsx.

### F-CX-R15-1-2 -- L1 card caption uses mutedForeground instead of mutedStrong
**Severity:** S1
**Where:** apps/mobile/src/domains/inbox/components/PendingOfferCard.tsx:172
**Evidence:** `          className="text-muted-foreground"`
**Rule:** 01-LAWS.md §4, Rule M mandates that any Caption or MetadataLabel on a tinted surface like surfaceAttention must use text-muted-strong.
**What the user sees:** The scheduled hours text lacks sufficient contrast on the tinted attention background.
**Fix:** Change the Caption className to text-muted-strong in PendingOfferCard.tsx since it only renders in the blocking state.

### F-CX-R15-1-3 -- Primary action button uses incorrect size and variant for its tier
**Severity:** S1
**Where:** apps/mobile/src/domains/inbox/components/PendingOfferCard.tsx:180
**Evidence:** `        <Button`
**Rule:** 01-LAWS.md §1 and §5G, Affordance grammar requires an L1 action to be size="lg", and the L3 rung dictates ghost or no action for routine cards.
**What the user sees:** A routine card has a loud, primary action button meant only for urgent tasks, and when urgent, the button is too small.
**Fix:** Add size="lg" when blocking and conditionally use variant="ghost" when not blocking in PendingOfferCard.tsx.

### F-CX-R15-1-4 -- L1 card body text uses mutedForeground instead of mutedStrong
**Severity:** S1
**Where:** apps/mobile/src/domains/inbox/components/NeedsAttentionCard.tsx:115
**Evidence:** `        <Body className="text-muted-foreground">`
**Rule:** 01-LAWS.md §1 and §4, The rung model and Rule M require body text and secondary labels on tinted grounds to use mutedStrong.
**What the user sees:** The "N more items" text lacks sufficient contrast when the card is in its tinted attention state.
**Fix:** Apply text-muted-strong conditionally when the card tone is attention in NeedsAttentionCard.tsx.

### F-CX-R15-1-5 -- Primary action button uses incorrect size and variant for its tier
**Severity:** S1
**Where:** apps/mobile/src/domains/inbox/components/NeedsAttentionCard.tsx:120
**Evidence:** `        <Button`
**Rule:** 01-LAWS.md §1 and §5G, Affordance grammar requires an L1 action to be size="lg", and the L3 rung dictates ghost or no action for feed cards.
**What the user sees:** The primary action button is too small for an L1 task, and remains loudly filled when demoted to an L3 feed card.
**Fix:** Add size="lg" when the tone is attention, and conditionally switch to variant="ghost" when the tone is default in NeedsAttentionCard.tsx.

### F-CX-R15-1-6 -- Inbox items use per-row elevation instead of a group card
**Severity:** S1
**Where:** apps/mobile/src/domains/inbox/components/InboxScreen.tsx:123
**Evidence:** `                  style={elevation.row}`
**Rule:** 01-LAWS.md §5D, Rule D dictates that L3-list rows must live inside ONE group card and have no per-row elevation, separated only by an inset hairline.
**What the user sees:** The inbox rows lack visual separation against the screen background because the row elevation shadow is too faint to be a border.
**Fix:** Wrap the inbox list items inside a single Card component and remove per-row elevation in InboxScreen.tsx.

CLEAN: apps/mobile/src/components/custom/BottomSheetBase.tsx, apps/mobile/src/app/(private)/(tabs)/_layout.tsx, apps/mobile/src/components/custom/useSheetDragToDismiss.ts, apps/mobile/src/components/custom/ErrorState.tsx
