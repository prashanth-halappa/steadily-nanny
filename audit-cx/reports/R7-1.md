I'm currently waiting for a background search to locate the exact path of the files in your codebase, as the relative paths weren't found in the initial directory check. Once the search finishes, I'll resume the audit.
### F-CX-R7-1-1 -- Toggle uses a filled primary button, resulting in two primary actions on screen
**Severity:** S1
**Where:** apps/mobile/src/domains/timesheet/components/WeekAdjustmentSheet.tsx:163
**Where:** apps/mobile/src/domains/timesheet/components/WeekAdjustmentSheet.tsx:176
**Evidence:** `              variant={direction === 'add' ? 'default' : 'outline'}`
**Rule:** 01-LAWS.md §5.G, "Filled default, full width, size="lg" | You owe someone this. One per screen."
**What the user sees:** Both the "Add" (or "Take off") toggle and the "Save" submit button render as filled primary actions simultaneously, breaking the single-primary-action rule.
**Fix:** Use `ChipToggle` or a secondary button variant for the direction selector in the WeekAdjustmentSheet component layer.

### F-CX-R7-1-2 -- The warning hue is used for text, failing contrast
**Severity:** S1
**Where:** apps/mobile/src/domains/timesheet/components/TimeEntryRow.tsx:142
**Evidence:** `              isZeroDuration && 'text-warning'`
**Rule:** 01-LAWS.md §3, "Threshold: 4.5:1 (3:1 only applies at ≥18.66px bold or ≥24px regular)" and Rule B "The ground carries the meaning; the words stay foreground"
**What the user sees:** The zero-duration time range text is printed in yellow on a white background, which is illegible and fails WCAG contrast minimums.
**Fix:** Change the class to `text-warning-ink` in the TimeEntryRow component layer.

### F-CX-R7-1-3 -- L3 row text uses Figure (28px), breaking hierarchy size limits
**Severity:** S1
**Where:** apps/mobile/src/domains/timesheet/components/TimeEntryRow.tsx:131
**Where:** apps/mobile/src/domains/timesheet/components/TimeEntryRow.tsx:176
**Evidence:** `          <Figure`
**Rule:** 01-LAWS.md §7, "Weight is non-decreasing with importance" and 00-FOUNDATIONS.md §4 table defining Figure as 28/34/700
**What the user sees:** The time range in an L3 routine row renders at 28px, which is larger than the H3 title (20px) of the L1 attention card above it, destroying the screen's visual hierarchy.
**Fix:** Replace `<Figure>` with `<Body tabular>` or a 16px equivalent in the TimeEntryRow component layer.

### F-CX-R7-1-4 -- L4 Context action uses an outline button instead of a text link
**Severity:** S1
**Where:** apps/mobile/src/domains/timesheet/components/WeekExportAction.tsx:194
**Evidence:** `        variant="outline"`
**Rule:** 01-LAWS.md §1, "L4 Context ... Action: text link"
**What the user sees:** The export action at the bottom of the screen renders as a full outlined button, inappropriately elevating an L4 action above a text link.
**Fix:** Change the button variant to a link style or use a text link in the WeekExportAction component layer.

CLEAN: apps/mobile/src/domains/timesheet/components/EarningsBreakdownSheet.tsx
