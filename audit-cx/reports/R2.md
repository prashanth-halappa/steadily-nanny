### F-CX-R2-1 -- Attention card missing required H3 title and filled action
**Severity:** S1
**Where:** apps/mobile/src/domains/today/components/NannyWeekLine.tsx:115
**Evidence:** `      <Card testID="today-week-line-card" tone="attention">`
**Rule:** 01-LAWS.md §3, "Every state change moves at least two... A card that goes to attention also raises its title to h3 and gains (or promotes) an action. tone alone is never enough."
**What the user sees:** An attention-tinted block that lacks the standard L1 structure, making its text look tiny (14px) and reading as a styling mistake rather than the highest priority item.
**Fix:** NannyWeekLine.tsx must upgrade the lineText wrapper to an H3 and provide a standard filled button for the action.

### F-CX-R2-2 -- Component hardcodes L1 tone, bypassing arbitration
**Severity:** S1
**Where:** apps/mobile/src/domains/today/components/NannyWeekLine.tsx:115
**Evidence:** `      <Card testID="today-week-line-card" tone="attention">`
**Rule:** 01-LAWS.md §2, "At most one L1 and one L2 exist per screen. When several items could claim L1, a single pure function decides — never a component."
**What the user sees:** If another item legitimately owns the pinned L1 slot, the user will see multiple competing L1 cards on the screen, violating the strict single-loudness-setting rule.
**Fix:** NannyWeekLine.tsx must use the standard default tone instead of hardcoding attention.

### F-CX-R2-3 -- L3 Routine card title uses Body instead of H4
**Severity:** S1
**Where:** apps/mobile/src/domains/today/components/TodayCoverage.tsx:291
**Evidence:** `            <Body weight="medium">{t('cover.setup.title')}</Body>`
**Rule:** 01-LAWS.md §1, the L3 Routine card must use typography.h4 for its title.
**What the user sees:** The setup card's title is smaller and lighter than standard card titles (16px medium instead of 18px semibold), breaking the typographic hierarchy and making it blend in with body copy.
**Fix:** TodayCoverage.tsx must replace the Body component with H4 for the setup card title.

### F-CX-R2-4 -- L4 Context bare ground block title uses Body instead of MetadataLabel
**Severity:** S1
**Where:** apps/mobile/src/domains/today/components/TodayCoverage.tsx:307
**Evidence:** `          <Body weight="medium">{t('cover.noNeed.title')}</Body>`
**Rule:** 01-LAWS.md §1, an L4 Context block on bare ground must use a MetadataLabel eyebrow for its title.
**What the user sees:** The no-need-today block title looks like regular body text rather than an eyebrow, violating the visual hierarchy for uncarded L4 context blocks.
**Fix:** TodayCoverage.tsx must replace the Body component with MetadataLabel for the noNeedToday block title.

### F-CX-R2-5 -- SplitTrack day bar omitted from gap state
**Severity:** S2
**Where:** apps/mobile/src/domains/today/components/TodayCoverage.tsx:335
**Evidence:** `      {state.status === 'booked' ? (`
**Rule:** screens-today.md §3.1, "The day bar. A SplitTrack (today-coverage-day-bar) at the top of the plan-lines block in both booked and gap"
**What the user sees:** When there is a coverage gap, the visual day bar disappears entirely, removing the at-a-glance timeline of the day right when the parent needs context.
**Fix:** TodayCoverage.tsx must adjust the condition so the SplitTrack renders in both booked and gap states.

### F-CX-R2-6 -- Small text on attention ground uses mutedForeground instead of mutedStrong
**Severity:** S1
**Where:** apps/mobile/src/domains/today/components/TodayCoverage.tsx:676
**Evidence:** `                    className="mt-2 text-muted-foreground"`
**Rule:** 01-LAWS.md §4 (Rule M), any secondary text on a tinted surface like surfaceAttention must use text-muted-strong to maintain contrast.
**What the user sees:** The disabled withdrawal reason text has poor contrast against the ochre attention background, making it hard to read and failing accessibility standards.
**Fix:** TodayCoverage.tsx must change the class to text-muted-strong for the withdrawReason text.

### F-CX-R2-7 -- Screen header has two context lines instead of one
**Severity:** S1
**Where:** apps/mobile/src/domains/today/components/TodayScreen.tsx:577
**Evidence:** `                <Body testID="today-lead" className="mt-1 text-muted-strong">`
**Rule:** 01-LAWS.md §5.C (Rule H), "Screen header: at most three elements — H1, ONE context line, ONE anchor figure."
**What the user sees:** The screen header contains four elements (H1, date line, lead line, and spot art anchor), making it taller and more cluttered than the system allows.
**Fix:** TodayScreen.tsx must consolidate or remove one of the context lines to strictly respect the three-element limit.

### F-CX-R2-8 -- Child chips rendered in feed instead of hero band
**Severity:** S1
**Where:** apps/mobile/src/domains/today/components/TodayScreen.tsx:615
**Evidence:** `            {isParentView ? (`
**Rule:** screens-today.md §2, the layout places the "[child chips row]" inside the HERO BAND on the wash, above the pinned slot.
**What the user sees:** The child chips appear in the standard feed below the urgent PinnedSlot, breaking the top-to-bottom hierarchy and separating them from the screen header.
**Fix:** TodayScreen.tsx must move the child chips block up into the ScreenHeader's anchor slot.

CLEAN: apps/mobile/src/app/(private)/(tabs)/home.tsx, apps/mobile/src/domains/today/components/CrossFamilyStrip.tsx, apps/mobile/src/domains/today/components/PinnedSlot.tsx, apps/mobile/src/domains/today/components/ThisWeekCard.tsx
