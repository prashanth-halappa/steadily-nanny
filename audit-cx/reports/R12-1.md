### F-CX-R12-1-1 -- Chip selections use alpha fills and borders instead of solid grounds
**Severity:** S1
**Where:** apps/mobile/src/domains/settings/components/NotificationPrefsScreen.tsx:270
apps/mobile/src/domains/settings/components/NotificationPrefsScreen.tsx:300
**Evidence:**                           ? 'rounded-chip border border-primary bg-primary/10 px-3 py-2'
**Rule:** 00-FOUNDATIONS.md §8.4, Selection must be conveyed with a solid opaque `bg-primary` fill and font weight, never via a translucent fill (`bg-primary/10`) or an outlined border.
**What the user sees:** The selected quiet hours chip renders as a faint tinted outline rather than a solid primary block, breaking the app's standard toggle affordance grammar.
**Fix:** Apply the standard `Chip` component classes (`bg-secondary` when unselected, `bg-primary` when selected) at the presentation layer in NotificationPrefsScreen.tsx.

### F-CX-R12-1-2 -- Selected chip text uses primary instead of primary-foreground
**Severity:** S1
**Where:** apps/mobile/src/domains/settings/components/NotificationPrefsScreen.tsx:277
apps/mobile/src/domains/settings/components/NotificationPrefsScreen.tsx:307
**Evidence:**                             ? 'text-primary'
**Rule:** 00-FOUNDATIONS.md §8.4, Text inside a selected chip must use `text-primary-foreground`, never `text-primary`.
**What the user sees:** The text inside a selected quiet hours chip is colored plum instead of white, which would fail contrast requirements if the ground were properly filled.
**Fix:** Change the selected text class to `text-primary-foreground` at the presentation layer in NotificationPrefsScreen.tsx.

### F-CX-R12-1-3 -- Section headers use Body instead of DayGroup or H2
**Severity:** S1
**Where:** apps/mobile/src/domains/settings/components/NotificationPrefsScreen.tsx:321
**Evidence:**           <Body weight="medium">{t('notificationPrefs.typesHeading')}</Body>
**Rule:** 01-LAWS.md §5.A, A named group inside a scrolling screen must render its header as `DayGroup` or `H2`, never as a body label.
**What the user sees:** The main notification preference section header is styled like slightly bolder body text, failing to properly establish the screen's visual hierarchy.
**Fix:** Change the `<Body weight="medium">` tag to `<DayGroup>` or `<H2>` in NotificationPrefsScreen.tsx.

### F-CX-R12-1-4 -- Section headers use Body instead of DayGroup or H2
**Severity:** S1
**Where:** apps/mobile/src/domains/settings/components/NotificationPrefsScreen.tsx:331
**Evidence:**               <Body weight="medium">{t(GROUP_I18N_KEY[group])}</Body>
**Rule:** 01-LAWS.md §5.A, A named group inside a scrolling screen must render its header as `DayGroup` or `H2`, never as a body label.
**What the user sees:** The notification preference group headers (Schedule, Hours & pay) are styled like slightly bolder body text, failing to visually separate the list sections.
**Fix:** Change the `<Body weight="medium">` tags to `<DayGroup>` or `<H2>` in NotificationPrefsScreen.tsx.

### F-CX-R12-1-5 -- className applied to an animated component
**Severity:** S1
**Where:** apps/mobile/src/domains/settings/components/NotificationPrefsScreen.tsx:268
apps/mobile/src/domains/settings/components/NotificationPrefsScreen.tsx:298
**Evidence:**                       className={
**Rule:** 00-FOUNDATIONS.md §7, Never put NativeWind `className` on a Reanimated `Animated.View` (or a component that wraps it); inline styles must be used to prevent overflow layout bugs.
**What the user sees:** The animated quiet hours chip might unexpectedly overflow its parent container or render as an overlay on top of unrelated UI.
**Fix:** Apply styling using inline `style` objects rather than `className` on the `AnimatedPressable` in NotificationPrefsScreen.tsx.

CLEAN: apps/mobile/src/app/(private)/settings/index.tsx
