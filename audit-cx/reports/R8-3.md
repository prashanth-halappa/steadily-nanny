### F-CX-R8-3-1 -- Translucent alpha fill used for an error surface
**Severity:** S1
**Where:** apps/mobile/src/domains/timesheet/components/PaymentCorrectionSheet.tsx:180
**Evidence:**               className="gap-2 rounded-cell bg-destructive/10 px-4 py-3"
**Rule:** 00-FOUNDATIONS.md §5.4 and §8.2, which forbid translucent alpha fills (e.g. `bg-*/NN`) for tinted surfaces and pills because they composite unpredictably against their backgrounds.
**What the user sees:** The refusal box renders with a transparent wash that blends with whatever is underneath, rather than a solid, reliable error tint.
**Fix:** PaymentCorrectionSheet.tsx should use the opaque `surfaceCritical` token for the background of the refusal block.

### F-CX-R8-3-2 -- Reject action uses ghost variant instead of a definitive second answer
**Severity:** S1
**Where:** apps/mobile/src/domains/expenses/components/ExpenseReviewSheet.tsx:193
**Evidence:**             variant="ghost"
**Rule:** 01-LAWS.md §5.G, which states that an equally valid second answer to a question must use `secondary` or `outline`, reserving `ghost` for optional or "not now" actions.
**What the user sees:** The "Reject" button appears visually demoted like an optional dismissal, despite being a definitive and equally valid response to the pending expense.
**Fix:** ExpenseReviewSheet.tsx should change the reject button's variant to `secondary` or `outline`.

### F-CX-R8-3-3 -- Per-row elevation applied to items in an L4 list
**Severity:** S1
**Where:** 
apps/mobile/src/domains/expenses/components/ReimbursementsCard.tsx:134
apps/mobile/src/domains/expenses/components/ReimbursementsCard.tsx:161
**Evidence:**           style={elevation.row}
**Rule:** 01-LAWS.md §1 (The rung model) and §5.D, which dictate that an L4 context uses a run of rows in one ListGroup and must never apply per-row `elevation.row`.
**What the user sees:** The individual reimbursement rows and the unavailable-total row render with separate shadows rather than appearing as a cohesive flat list.
**Fix:** ReimbursementsCard.tsx should remove `elevation.row` from individual rows and rely on a parent list group for separation.

### F-CX-R8-3-4 -- Per-row elevation applied to the total row in an L4 list
**Severity:** S1
**Where:** apps/mobile/src/domains/expenses/components/ReimbursementsCard.tsx:149
**Evidence:**         <View className="rounded-row bg-card px-4 py-3" style={elevation.row}>
**Rule:** 01-LAWS.md §1 (The rung model) and §5.D, which state that an L4 list must never use per-row `elevation.row`.
**What the user sees:** The total row is visually detached with its own shadow, breaking the visual grouping of the L4 list.
**Fix:** ReimbursementsCard.tsx should remove the inline `elevation.row` style from the total row.

### F-CX-R8-3-5 -- Per-row elevation applied to an L4 payment record
**Severity:** S1
**Where:** apps/mobile/src/domains/timesheet/components/PaymentRow.tsx:70
**Evidence:**       style={[elevation.row, { minHeight: ROW_MIN_HEIGHT }]}
**Rule:** 01-LAWS.md §1 (The rung model), which states that an L4 context (like this record row) must never use per-row `elevation.row`.
**What the user sees:** The payment row renders with a shadow, treating it as an individual lifting card rather than a flat row in a ledger list.
**Fix:** PaymentRow.tsx should drop `elevation.row` and rely on its parent list for separation.

CLEAN: apps/mobile/src/domains/expenses/components/PendingExpensesRow.tsx, apps/mobile/src/domains/expenses/components/ExpenseRow.tsx
