### F-CX-R11-1-1 -- L4 rows have per-row elevation instead of sharing a ListGroup card
**Severity:** S1
**Where:** apps/mobile/src/domains/draft/components/InviteRow.tsx:87
**Evidence:** `      style={elevation.row}`
**Rule:** 01-LAWS.md §1 (The rung model) and §5.D (L3-list) — L4 rows must live in ONE card with no per-row elevation
**What the user sees:** The invite rows lift individually with their own shadows instead of appearing as a single dense list within one card.
**Fix:** Remove `elevation.row` and `rounded-row bg-card` from `InviteRow.tsx`, and wrap the list in a single `Card` in `DraftHomeScreen.tsx`.

### F-CX-R11-1-2 -- The Terms card uses a category icon chip when promoted to L1
**Severity:** S1
**Where:** apps/mobile/src/domains/draft/components/DraftHomeScreen.tsx:209
**Evidence:** `          <IconChip tone="hours" icon={Wallet} />`
**Rule:** 00-FOUNDATIONS.md §3.3 and §8.1 — A card at L1 (`tone="attention"`) must drop its category chip and use `brand` (plum) instead
**What the user sees:** The urgent "Your terms" card incorrectly shows a sage green category icon on an ochre attention ground, violating the single-register rule.
**Fix:** Conditionally pass `tone="brand"` to `IconChip` when `hasTerms` is false in `DraftHomeScreen.tsx`.

### F-CX-R11-1-3 -- Muted eyebrow on the screen wash uses the wrong contrast token
**Severity:** S1
**Where:** apps/mobile/src/domains/draft/components/DraftHomeScreen.tsx:402
**Evidence:** `                <MetadataLabel className="text-muted-foreground">`
**Rule:** 01-LAWS.md §4 (Rule M) — `mutedForeground` fails accessibility on a wash; `mutedStrong` must be used
**What the user sees:** The "Sent to" eyebrow text has insufficient contrast against the plum-tinted screen wash.
**Fix:** Change the class to `text-muted-strong` on the `MetadataLabel` in `DraftHomeScreen.tsx`.

### F-CX-R11-1-4 -- Small text on a plain L3 card incorrectly uses mutedStrong
**Severity:** S1
**Where:** apps/mobile/src/domains/draft/components/DraftHomeScreen.tsx:172
**Where:** apps/mobile/src/domains/draft/components/DraftHomeScreen.tsx:178
**Where:** apps/mobile/src/domains/draft/components/DraftHomeScreen.tsx:183
**Evidence:** `          <Small testID="draft-share-offline" className="text-muted-strong">`
**Evidence:** `            className="text-muted-strong"`
**Evidence:** `          <Small testID="draft-share-needs-terms" className="text-muted-strong">`
**Rule:** 01-LAWS.md §4 (Rule M) — On plain card backgrounds, `mutedForeground` must be used instead of `mutedStrong`
**What the user sees:** Secondary hint text on the share card is unnecessarily dark when the card is in its white L3 state.
**Fix:** Apply `text-muted-strong` only when `shareIsL1` is true, and `text-muted-foreground` when false in `DraftHomeScreen.tsx`.

CLEAN: apps/mobile/src/domains/draft/components/DraftTermsScreen.tsx, apps/mobile/src/domains/draft/components/ShareTermsSheet.tsx
