I'm looking for the repository on your machine and will start the audit as soon as I find it! I have a background search running to locate the design documents and the mobile app codebase. I'll get back to you momentarily.
### F-CX-R9a-2-1 -- Chip toggles use outline styling instead of weight and primary fill
**Severity:** S1
**Where:** apps/mobile/src/domains/pay/components/PayScheduleFields.tsx:103
apps/mobile/src/domains/pay/components/PayScheduleFields.tsx:136
**Evidence:** `                selected && 'border-primary bg-secondary'`
**Rule:** 00-FOUNDATIONS.md §8.4, Chip selection must use a primary fill and text, never a secondary fill with a primary outline.
**What the user sees:** Selected schedule chips look like active outline buttons rather than solid selected pills.
**Fix:** Update the selected classes to use `bg-primary`, `text-primary-foreground`, and bold weight in PayScheduleFields.tsx.

### F-CX-R9a-2-2 -- Chip toggles use literal radius tokens instead of semantic ones
**Severity:** S3
**Where:** apps/mobile/src/domains/pay/components/PayScheduleFields.tsx:102
apps/mobile/src/domains/pay/components/PayScheduleFields.tsx:135
**Evidence:** `                'rounded-full border border-input px-3 py-2',`
**Rule:** 00-FOUNDATIONS.md §1, Soft radii must use semantic tokens like `rounded-chip` for pills and chips, never literal geometry like `rounded-full`.
**What the user sees:** The chips have round ends but break the semantic design contract, risking drift if the system's pill geometry ever changes.
**Fix:** Replace `rounded-full` with `rounded-chip` in PayScheduleFields.tsx.

### F-CX-R9a-2-3 -- Cancellation field uses primary buttons instead of chip toggles
**Severity:** S1
**Where:** apps/mobile/src/domains/pay/components/CancellationTermField.tsx:64
apps/mobile/src/domains/pay/components/CancellationTermField.tsx:76
**Evidence:** `          variant={choice === 'window' ? 'default' : 'outline'}`
**Rule:** 01-LAWS.md §5.G, A filled default button means "You owe someone this" and cannot be used as a toggle state for a choice.
**What the user sees:** The selected cancellation option renders as a primary action button, implying submission rather than a form choice.
**Fix:** Replace the Button components with standard Chip styling in CancellationTermField.tsx.

### F-CX-R9a-2-4 -- Currency dropdown list separated by banned border
**Severity:** S1
**Where:** apps/mobile/src/domains/pay/components/CurrencySelect.tsx:196
**Evidence:** `            className="overflow-hidden rounded-2xl border border-border"`
**Rule:** 01-LAWS.md §6 and 00-FOUNDATIONS.md §5.2, Surfaces must be separated by elevation shadows, not hairline borders.
**What the user sees:** The expanded currency list is bordered by a grey rule rather than a shadow, breaking the system's depth model.
**Fix:** Remove the border classes and apply elevation styling to the list container in CurrencySelect.tsx.

### F-CX-R9a-2-5 -- Currency dropdown trigger uses literal radius tokens instead of semantic ones
**Severity:** S3
**Where:** apps/mobile/src/domains/pay/components/CurrencySelect.tsx:173
**Evidence:** `        className="h-12 flex-row items-center justify-between rounded-2xl border border-input bg-background px-4"`
**Rule:** 00-FOUNDATIONS.md §1, Soft radii must use semantic tokens like `rounded-card` or `rounded-button`, not literal values like `rounded-2xl`.
**What the user sees:** The currency select trigger has a 20px radius but breaks the system's semantic contract, risking drift.
**Fix:** Change `rounded-2xl` to a semantic token like `rounded-card` in CurrencySelect.tsx.

### F-CX-R9a-2-6 -- Currency dropdown list uses literal radius tokens instead of semantic ones
**Severity:** S3
**Where:** apps/mobile/src/domains/pay/components/CurrencySelect.tsx:196
**Evidence:** `            className="overflow-hidden rounded-2xl border border-border"`
**Rule:** 00-FOUNDATIONS.md §1, Soft radii must use semantic tokens like `rounded-card`, not literal values like `rounded-2xl`.
**What the user sees:** The currency list dropdown has a 20px radius but breaks the system's semantic contract.
**Fix:** Change `rounded-2xl` to a semantic token like `rounded-card` in CurrencySelect.tsx.

### F-CX-R9a-2-7 -- Date field errors use wrong destructive text color
**Severity:** S1
**Where:** apps/mobile/src/domains/pay/components/EffectiveDateField.tsx:109
apps/mobile/src/domains/pay/components/EffectiveDateField.tsx:117
**Evidence:** `          className="text-destructive"`
**Rule:** 00-FOUNDATIONS.md §3.2, `errorInlineText` is the required token for destructive text; the base `destructive` token must not be used as text.
**What the user sees:** The error messages appear in the base destructive red instead of the deeper error-inline-text ink designed for legibility on a card.
**Fix:** Change `text-destructive` to `text-error-inline-text` in EffectiveDateField.tsx.

### F-CX-R9a-2-8 -- TermGroup header uses wrong IconChip size
**Severity:** S3
**Where:** apps/mobile/src/domains/pay/components/TermGroup.tsx:92
**Evidence:** `        <IconChip tone="hours" icon={icon} />`
**Rule:** screens-pay-terms.md §4.2, The term group's icon chip must be 24px, overriding the 28px component default.
**What the user sees:** The icon chip in the group header is slightly larger than specified, throwing off the row's vertical rhythm.
**Fix:** Pass a size prop of 24 to the IconChip component in TermGroup.tsx.

CLEAN: apps/mobile/src/domains/pay/components/PayTermsRequiredCore.tsx, apps/mobile/src/domains/pay/components/AmountRow.tsx, apps/mobile/src/domains/pay/components/EffectiveDateField.utils.ts
