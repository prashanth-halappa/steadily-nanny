### F-CX-R16-1c-1 -- Dialog uses page ground colour for a container
**Severity:** S1
**Where:** apps/mobile/src/components/ui/alert-dialog.tsx:82
**Evidence:** `            'z-50 max-w-lg gap-4 border border-border bg-background p-6 shadow-lg shadow-foreground/10 web:duration-200 rounded-lg',`
**Rule:** 00-FOUNDATIONS.md §1, `background` is exclusively for the page ground; containers must use a surface token like `card` or `popover`.
**What the user sees:** The dialog blends into the background of the screen if shadows fail or on low-contrast monitors.
**Fix:** In apps/mobile/src/components/ui/alert-dialog.tsx, replace `bg-background` with `bg-card`.

### F-CX-R16-1c-2 -- Dialog mixes border and shadow separation
**Severity:** S1
**Where:** apps/mobile/src/components/ui/alert-dialog.tsx:82
**Evidence:** `            'z-50 max-w-lg gap-4 border border-border bg-background p-6 shadow-lg shadow-foreground/10 web:duration-200 rounded-lg',`
**Rule:** 01-LAWS.md §6, surfaces separate by light (shadow) or by rule (border), never both on the same card.
**What the user sees:** The dialog appears unusually heavy, violating the single separation system chosen for the app.
**Fix:** In apps/mobile/src/components/ui/alert-dialog.tsx, remove either `border border-border` or the shadow classes.

### F-CX-R16-1c-3 -- Dialog uses an arbitrary radius instead of the card token
**Severity:** S1
**Where:** apps/mobile/src/components/ui/alert-dialog.tsx:82
**Evidence:** `            'z-50 max-w-lg gap-4 border border-border bg-background p-6 shadow-lg shadow-foreground/10 web:duration-200 rounded-lg',`
**Rule:** 00-FOUNDATIONS.md §4, arbitrary values are forbidden and cards must use the `rounded-card` token.
**What the user sees:** The dialog corners have a different radius than the rest of the application's cards.
**Fix:** In apps/mobile/src/components/ui/alert-dialog.tsx, replace `rounded-lg` with `rounded-card`.

### F-CX-R16-1c-4 -- Dialog uses an in-card gap larger than the maximum
**Severity:** S2
**Where:** apps/mobile/src/components/ui/alert-dialog.tsx:82
**Evidence:** `            'z-50 max-w-lg gap-4 border border-border bg-background p-6 shadow-lg shadow-foreground/10 web:duration-200 rounded-lg',`
**Rule:** 01-LAWS.md §5, in-card gaps must be ≤ 12px.
**What the user sees:** The spacing between elements inside the dialog is too loose, weakening the visual relationship of its contents.
**Fix:** In apps/mobile/src/components/ui/alert-dialog.tsx, change `gap-4` to `gap-3` (12px) or less.

### F-CX-R16-1c-5 -- Dialog uses arbitrary padding instead of card padding
**Severity:** S2
**Where:** apps/mobile/src/components/ui/alert-dialog.tsx:82
**Evidence:** `            'z-50 max-w-lg gap-4 border border-border bg-background p-6 shadow-lg shadow-foreground/10 web:duration-200 rounded-lg',`
**Rule:** 00-FOUNDATIONS.md §3, cards must use the `spacing.cardPadding` token (16px).
**What the user sees:** The dialog has more internal padding than standard cards, creating an inconsistent rhythm.
**Fix:** In apps/mobile/src/components/ui/alert-dialog.tsx, replace `p-6` with `p-4` (or the equivalent `spacing.cardPadding` class).

### F-CX-R16-1c-6 -- Dialog web animation uses incorrect duration
**Severity:** S3
**Where:** apps/mobile/src/components/ui/alert-dialog.tsx:82
**Evidence:** `            'z-50 max-w-lg gap-4 border border-border bg-background p-6 shadow-lg shadow-foreground/10 web:duration-200 rounded-lg',`
**Rule:** 00-FOUNDATIONS.md §6, state changes must use the normal duration of 250ms.
**What the user sees:** The web dialog animates slightly faster than the rest of the application's standard transitions.
**Fix:** In apps/mobile/src/components/ui/alert-dialog.tsx, change `web:duration-200` to a duration class that matches 250ms.

### F-CX-R16-1c-7 -- Dialog scales on entrance instead of fading
**Severity:** S1
**Where:** apps/mobile/src/components/ui/alert-dialog.tsx:84
**Evidence:** `              ? 'web:animate-in web:fade-in-0 web:zoom-in-95'`
**Rule:** 00-FOUNDATIONS.md §6, state changes and fades must use opacity or height only, with no scale or bounce.
**What the user sees:** The dialog visually pounces or zooms when opening, rather than settling calmly.
**Fix:** In apps/mobile/src/components/ui/alert-dialog.tsx, remove `web:zoom-in-95`.

### F-CX-R16-1c-8 -- Dialog scales on exit instead of fading
**Severity:** S1
**Where:** apps/mobile/src/components/ui/alert-dialog.tsx:85
**Evidence:** `              : 'web:animate-out web:fade-out-0 web:zoom-out-95',`
**Rule:** 00-FOUNDATIONS.md §6, state changes and fades must use opacity or height only, with no scale or bounce.
**What the user sees:** The dialog visibly shrinks when closing, which contradicts the app's motion rules.
**Fix:** In apps/mobile/src/components/ui/alert-dialog.tsx, remove `web:zoom-out-95`.

### F-CX-R16-1c-9 -- Dialog native overlay entrance animation is too fast
**Severity:** S3
**Where:** apps/mobile/src/components/ui/alert-dialog.tsx:53
**Evidence:** `        entering={FadeIn.duration(150)}`
**Rule:** 00-FOUNDATIONS.md §6, expands and fades must use the normal duration of 250ms, not the fast duration of 150ms.
**What the user sees:** The native dialog overlay fades in too quickly, breaking the app's standard motion pacing.
**Fix:** In apps/mobile/src/components/ui/alert-dialog.tsx, change `150` to `250` or use the system's `ANIMATION_DURATION.normal`.

### F-CX-R16-1c-10 -- Dialog native overlay exit animation is too fast
**Severity:** S3
**Where:** apps/mobile/src/components/ui/alert-dialog.tsx:54
**Evidence:** `        exiting={FadeOut.duration(150)}`
**Rule:** 00-FOUNDATIONS.md §6, expands and fades must use the normal duration of 250ms, not the fast duration of 150ms.
**What the user sees:** The native dialog overlay vanishes too quickly.
**Fix:** In apps/mobile/src/components/ui/alert-dialog.tsx, change `150` to `250` or use the system's `ANIMATION_DURATION.normal`.

### F-CX-R16-1c-11 -- Title uses numeric font weight instead of font family
**Severity:** S1
**Where:** apps/mobile/src/components/ui/alert-dialog.tsx:120
**Evidence:** `        'text-lg native:text-xl text-foreground font-semibold',`
**Rule:** 00-FOUNDATIONS.md §2, typography weight is selected by `fontFamily`, never by numeric `fontWeight` classes.
**What the user sees:** The dialog title renders in a fallback system font or the wrong weight on devices where the custom font isn't mapped to standard weights.
**Fix:** In apps/mobile/src/components/ui/alert-dialog.tsx, replace `font-semibold` and text sizes with the appropriate typography token like `typography.h3`.

CLEAN: docs/design/00-FOUNDATIONS.md, docs/design/01-LAWS.md, docs/design/02-VOICE.md, docs/design/03-ART-DIRECTION.md
The background task I started earlier to locate the repository has finished. I have already completed the audit using the files it found in the `/Users/prashanthhalappa/Documents/MonorepoTemplate/` directory and provided the findings in the previous response. 

Let me know if you need me to audit any other components or if you have any questions about these findings!
