### F-CX-R9a-1-1 -- "The rest of the agreement" uses the wrong typography primitive
**Severity:** S1
**Where:** apps/mobile/src/domains/pay/components/PayTermsGroups.tsx:381
**Evidence:** `      <Small className="px-1 text-muted-foreground">`
**Rule:** screens-pay-terms.md section 4.1, The label "The rest of the agreement" must be rendered as a `MetadataLabel`.
**What the user sees:** The section heading above the term groups renders as ordinary small body text instead of a structural eyebrow.
**Fix:** Change the `<Small>` component to `<MetadataLabel>` in PayTermsGroups.tsx.

### F-CX-R9a-1-2 -- Guaranteed hours group is missing the daily overtime assumption warning
**Severity:** S0
**Where:** apps/mobile/src/domains/pay/components/PayTermsGroups.tsx:643
**Evidence:** `          ) : null}`
**Rule:** screens-pay-terms.md section 10, The guaranteed hours weekly equivalent must be followed by a `Small mutedForeground` line stating that the calculation assumes five 10-hour days when daily overtime is set.
**What the user sees:** A parent using daily overtime sees a flat weekly equivalent figure without being told it assumes an even 10-hour day spread, leading to incorrect expectations of total cost.
**Fix:** Add the conditional `Small` warning below the weekly equivalent `Body` in PayTermsGroups.tsx.

### F-CX-R9a-1-3 -- Holidays group is missing the list of paid holidays
**Severity:** S0
**Where:** apps/mobile/src/domains/pay/components/PayTermsGroups.tsx:696
**Evidence:** `        <View className="gap-2">`
**Rule:** screens-pay-terms.md section 4.3, The Holidays group must contain a list of holidays with toggle switches above the worked-holiday premium field.
**What the user sees:** The user is unable to choose which holidays are paid directly on the terms form, hiding a required part of the agreement.
**Fix:** Add the household-level holiday toggle list inside the Holidays TermGroup in PayTermsGroups.tsx.

### F-CX-R9a-1-4 -- The Pay schedule group is completely missing
**Severity:** S0
**Where:** apps/mobile/src/domains/pay/components/PayTermsGroups.tsx:856
**Evidence:** `      </TermGroup>`
**Rule:** screens-pay-terms.md section 4.3, The Pay schedule group (frequency and pay day) must be present in the form as its own `TermGroup`.
**What the user sees:** The user has no way to configure how often they pay or the pay day, blocking them from completing the pay schedule term of the agreement.
**Fix:** Implement the Pay schedule TermGroup and insert it between the Outside wages and In writing groups in PayTermsGroups.tsx.

CLEAN: 
