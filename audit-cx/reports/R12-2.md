### F-CX-R12-2-1 -- ChipToggle selection uses a translucent alpha fill instead of solid brand color
**Severity:** S1
**Where:** apps/mobile/src/domains/settings/components/TimeSettingsScreen.tsx:212
**Evidence:** `                      ? 'border-primary bg-primary/10 text-primary'`
**Rule:** 00-FOUNDATIONS.md §8.4, ChipToggle selection must be weight and fill together (bg-primary + text-primary-foreground), never an alpha fill alone.
**What the user sees:** The selected weekday chip has a faint, translucent plum background and colored text instead of the required strong, solid brand block.
**Fix:** Update the class string in TimeSettingsScreen.tsx to use bg-primary, text-primary-foreground, and font-semibold when selected.

### F-CX-R12-2-2 -- Literal string uses forbidden uppercase characters
**Severity:** S1
**Where:** apps/mobile/src/app/(private)/debug.tsx:58
**Evidence:** `        <Small className="text-muted-foreground">VERIFY: {assertion}</Small>`
**Rule:** 00-FOUNDATIONS.md §4, Typography requires sentence case everywhere and forbids any uppercase micro-labels.
**What the user sees:** The word "VERIFY:" is capitalized, breaking the app's calm, sentence-case voice constraint.
**Fix:** Change the literal string to "Verify:" in the layout layer of debug.tsx.

### F-CX-R12-2-3 -- Section header uses a demoted annotation style instead of a structural heading
**Severity:** S1
**Where:** apps/mobile/src/domains/settings/components/CalendarPickerSheet.tsx:139
**Evidence:** `            <Small className="text-muted-foreground">{group.sourceName}</Small>`
**Rule:** 01-LAWS.md §5 A, A named group inside a scrolling screen must render its header as DayGroup or H2, and never as a demoted label like Small or MetadataLabel.
**What the user sees:** The calendar source header (e.g. "iCloud") appears too small and faint to structurally group the list items below it.
**Fix:** Replace the Small component with a DayGroup or H2 component in the layout layer of CalendarPickerSheet.tsx.

### F-CX-R12-2-4 -- Dense list rows rendered as separate disjointed cards instead of a unified group card
**Severity:** S1
**Where:** apps/mobile/src/app/(private)/settings/carer-availability.tsx:140
**Where:** apps/mobile/src/app/(private)/settings/household-time-off.tsx:104
**Evidence:** `        <View className="mt-4 gap-2">`
**Rule:** 01-LAWS.md §5 D, Rows in an L3 dense list must live inside ONE default-tone Card component and separate by an inset hairline, never as individually lifted rows with gaps.
**What the user sees:** The list items render as a stack of disconnected blocks with gaps between them instead of a single grouped card.
**Fix:** Wrap the mapped list items in a single grouped Card component and remove the gap in both screen components.

CLEAN: apps/mobile/src/app/(private)/settings/edit-name.tsx, apps/mobile/src/app/(private)/settings/join-household.tsx, apps/mobile/src/app/(private)/settings/notifications.tsx, apps/mobile/src/app/(private)/settings/time.tsx
