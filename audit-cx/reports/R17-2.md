### F-CX-R17-2-1 -- Skeleton shimmer adds a banned top border accent
**Severity:** S1
**Where:** apps/mobile/src/components/ui/skeleton-shimmer.tsx:47
**Evidence:** `                borderTopWidth: 2,`
**Rule:** 01-LAWS.md section 6, the ban on card borders and accent bars stands with only the inset hairline as an exception.
**What the user sees:** The skeleton placeholder has a top accent bar that disappears when the actual card renders, causing the UI to pop visually on load.
**Fix:** Remove the dimensionColor border property from the SkeletonShimmer animated style in skeleton-shimmer.tsx.

### F-CX-R17-2-2 -- Skeleton animates opacity instead of crossfading colours
**Severity:** S1
**Where:** apps/mobile/src/components/ui/skeleton-shimmer.tsx:37
**Evidence:** `    opacity.value = withRepeat(`
**Rule:** 00-FOUNDATIONS.md section 8.8, skeletons must shimmer from skeletonBase to skeletonHighlight #FFFFFF.
**What the user sees:** The skeleton fades against its background rather than shimmering with a white highlight.
**Fix:** Change the Reanimated style to interpolate backgroundColor instead of opacity in skeleton-shimmer.tsx.

### F-CX-R17-2-3 -- Day header uses 24px top spacing instead of the required 32px
**Severity:** S1
**Where:** apps/mobile/src/components/ui/day-header.tsx:32
**Evidence:** `      className="flex-row items-center justify-between px-5.5 pt-6 pb-2"`
**Rule:** 01-LAWS.md section 5.B, space above a group header must be ~4x the space below it (32px / pt-8).
**What the user sees:** The section header sits too close to the previous section, breaking the vertical rhythm.
**Fix:** Update the padding class in day-header.tsx from pt-6 to pt-8.

### F-CX-R17-2-4 -- IconChip sm size creates a banned circular chip
**Severity:** S1
**Where:** apps/mobile/src/components/ui/icon-chip.tsx:59
**Evidence:** `  sm: { dimensionClass: 'h-6 w-6', iconSize: 14 },`
**Rule:** 00-FOUNDATIONS.md section 1, icon chips must use rounded-cell (12px) to read as a square and never a circle.
**What the user sees:** The 24px chip with a 12px radius renders as a perfect circle, confusing it with an avatar.
**Fix:** Remove the sm size or adjust its radius in icon-chip.tsx so it remains a rounded square.

### F-CX-R17-2-5 -- AnimatedPressable uses a banned NativeWind className
**Severity:** S0
**Where:** apps/mobile/src/components/ui/week-nav-header.tsx:58
**Evidence:** `        className={isPreviousDisabled ? 'opacity-40' : undefined}`
**Rule:** 00-FOUNDATIONS.md section 7, never put NativeWind className on an Animated component.
**What the user sees:** The animated chevron button silently overflows its container and renders as an overlay on top of unrelated UI.
**Fix:** Move the conditional opacity into the inline style array in week-nav-header.tsx.

### F-CX-R17-2-6 -- AnimatedPressable uses a banned NativeWind className
**Severity:** S0
**Where:** apps/mobile/src/components/ui/week-nav-header.tsx:81
**Evidence:** `        className={isNextDisabled ? 'opacity-40' : undefined}`
**Rule:** 00-FOUNDATIONS.md section 7, never put NativeWind className on an Animated component.
**What the user sees:** The animated chevron button silently overflows its container and renders as an overlay on top of unrelated UI.
**Fix:** Move the conditional opacity into the inline style array in week-nav-header.tsx.

### F-CX-R17-2-7 -- AnimatedPressable uses subtle scale intensity instead of standard
**Severity:** S1
**Where:** apps/mobile/src/components/ui/week-nav-header.tsx:49
**Evidence:** `        scaleIntensity="subtle"`
**Rule:** 00-FOUNDATIONS.md section 10, press motion must use scaleIntensity="standard".
**What the user sees:** The button scales less than other standard pressable elements when tapped, breaking motion consistency.
**Fix:** Change scaleIntensity to "standard" in week-nav-header.tsx.

### F-CX-R17-2-8 -- AnimatedPressable uses subtle scale intensity instead of standard
**Severity:** S1
**Where:** apps/mobile/src/components/ui/week-nav-header.tsx:72
**Evidence:** `        scaleIntensity="subtle"`
**Rule:** 00-FOUNDATIONS.md section 10, press motion must use scaleIntensity="standard".
**What the user sees:** The button scales less than other standard pressable elements when tapped, breaking motion consistency.
**Fix:** Change scaleIntensity to "standard" in week-nav-header.tsx.

### F-CX-R17-2-9 -- Badge includes an outlined variant
**Severity:** S1
**Where:** apps/mobile/src/components/ui/badge.tsx:26
**Evidence:** `        outline: 'border border-border text-foreground',`
**Rule:** 01-LAWS.md section 6, the ban on card borders, list hairlines and accent bars stands.
**What the user sees:** An outlined badge appears with a hairline border, violating the shadow-instead-of-rule inversion.
**Fix:** Remove the outline variant from the Badge component in badge.tsx.

### F-CX-R17-2-10 -- BlockQuote uses a banned accent bar
**Severity:** S1
**Where:** apps/mobile/src/components/ui/typography/body.tsx:62
**Evidence:** `        'mt-6 native:mt-4 border-l-2 border-border pl-6 native:pl-3 text-foreground italic web:select-text',`
**Rule:** 01-LAWS.md section 6, the ban on card borders, list hairlines and accent bars stands.
**What the user sees:** The blockquote renders with a left border stripe, which was explicitly removed for failing the soft-radii geometry.
**Fix:** Remove the border-l-2 class from BlockQuote in body.tsx.

### F-CX-R17-2-11 -- StatusPill includes an uncovered variant that does not represent a decision
**Severity:** S1
**Where:** apps/mobile/src/components/ui/status-pill.tsx:22
**Evidence:** `        uncovered: 'bg-pill-warning',`
**Rule:** 01-LAWS.md section 5.G, StatusPill must state what someone else decided and never anything else.
**What the user sees:** An unattended 'uncovered' state is presented using a component reserved for explicit decisions made by another person.
**Fix:** Remove the uncovered variant from StatusPill in status-pill.tsx.

### F-CX-R17-2-12 -- StatusPill includes an uncovered text variant that does not represent a decision
**Severity:** S1
**Where:** apps/mobile/src/components/ui/status-pill.tsx:38
**Evidence:** `      uncovered: 'text-warning-ink',`
**Rule:** 01-LAWS.md section 5.G, StatusPill must state what someone else decided and never anything else.
**What the user sees:** An unattended 'uncovered' state is presented using a component reserved for explicit decisions made by another person.
**Fix:** Remove the uncovered text variant from StatusPill in status-pill.tsx.

CLEAN: apps/mobile/src/components/ui/typography/index.ts
