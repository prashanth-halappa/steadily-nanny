### F-CX-R8-1-1 -- Month header uses incorrect top padding for a group header
**Severity:** S1
**Where:** apps/mobile/src/domains/timesheet/components/PaymentsScreen.tsx:180
**Evidence:**       className="flex-row items-center justify-between gap-3 pt-6 pb-2"
**Rule:** 01-LAWS.md 5.B Rhythm, "Space above a group header is ~4× the space below it. Above: 32px (pt-8). Below: 8px (pb-2)."
**What the user sees:** The space above the month header is too small, breaking the visual rhythm of the screen and reducing separation from the previous section.
**Fix:** Change pt-6 to pt-8 in the class string in apps/mobile/src/domains/timesheet/components/PaymentsScreen.tsx.

### F-CX-R8-1-2 -- Month header skeleton uses incorrect top padding for a group header
**Severity:** S1
**Where:** apps/mobile/src/domains/timesheet/components/PaymentsScreen.tsx:144
**Evidence:**       <View className="pt-6 pb-2">
**Rule:** 01-LAWS.md 5.B Rhythm, "Space above a group header is ~4× the space below it. Above: 32px (pt-8). Below: 8px (pb-2)."
**What the user sees:** The skeleton space above the month header is too small, matching the incorrect pt-6 rhythm.
**Fix:** Change pt-6 to pt-8 in the class string in apps/mobile/src/domains/timesheet/components/PaymentsScreen.tsx.

### F-CX-R8-1-3 -- List skeleton uses detached per-row cards instead of ONE ListGroup
**Severity:** S1
**Where:** apps/mobile/src/domains/timesheet/components/PaymentsScreen.tsx:154
**Evidence:**           className="mb-2 justify-center rounded-row bg-card px-4 py-3"
**Rule:** 01-LAWS.md 5.D L3-list, "Rows live inside ONE Card tone="default" p-0 overflow-hidden; the card lifts, the rows do not. No per-row elevation."
**What the user sees:** The skeleton rows render as detached individual blocks with gaps instead of a single contiguous list group, misrepresenting the hierarchy.
**Fix:** Replace per-row bg-card and mb-2 with a single containing Card block in apps/mobile/src/domains/timesheet/components/PaymentsScreen.tsx.

### F-CX-R8-1-4 -- Export button uses incorrect scaleIntensity for press motion
**Severity:** S1
**Where:** apps/mobile/src/domains/timesheet/components/PaymentsScreen.tsx:409
**Evidence:**           scaleIntensity="subtle"
**Rule:** 00-FOUNDATIONS.md 10 Motion, "Press | AnimatedPressable scaleIntensity="standard" + haptic="light""
**What the user sees:** The export button scales less visibly on press than standard interactive elements across the app.
**Fix:** Change scaleIntensity to "standard" in apps/mobile/src/domains/timesheet/components/PaymentsScreen.tsx.

### F-CX-R8-1-5 -- Screen header contains too many elements
**Severity:** S1
**Where:** apps/mobile/src/domains/timesheet/components/PaymentsScreen.tsx:428
**Evidence:**         <Caption testID="payments-read-only" className="text-muted-strong">
**Rule:** 01-LAWS.md 5.C Screen header (Rule H), "At most three elements: H1, ONE context line, ONE anchor."
**What the user sees:** The screen header stacks four elements (H1, subtitle, household switcher, read-only note), creating a crowded band that violates the three-element limit.
**Fix:** Move the read-only note or household switcher out of the header band in apps/mobile/src/domains/timesheet/components/PaymentsScreen.tsx.

### F-CX-R8-1-6 -- Unpaid badge uses outlined style instead of filled
**Severity:** S1
**Where:** apps/mobile/src/domains/timesheet/components/PaidStateSection.tsx:82
**Evidence:**   unpaid: 'outline',
**Rule:** 00-FOUNDATIONS.md 8.2 StatusPill, "Filled, never outlined. StatusPill states what someone else decided... it is never a control."
**What the user sees:** The unpaid status badge renders with an outline, making it look like an interactive control rather than a settled fact.
**Fix:** Change 'outline' to a filled variant in apps/mobile/src/domains/timesheet/components/PaidStateSection.tsx.

CLEAN:
