### F-CX-R17-1-1 -- Typography factory downgrades Figure token weight and size
**Severity:** S1
**Where:** apps/mobile/src/components/ui/typography/factory.tsx:127
**Evidence:** `    weight: '400',`
**Rule:** 00-FOUNDATIONS.md 4, the figure token must be 28/34/700 tabular, so defining it as 400 violates the non-decreasing weight rule.
**What the user sees:** Card-level numbers appear at a light weight instead of standing out with the intended bold hierarchy.
**Fix:** Rename this 16px factory to BodyTabular in apps/mobile/src/components/ui/typography/factory.tsx.

### F-CX-R17-1-2 -- Selected WeekStrip day uses rounded-cell instead of pill
**Severity:** S2
**Where:** apps/mobile/src/components/ui/week-strip.tsx:111
**Evidence:** `              'h-touch w-touch items-center justify-center gap-0.5 rounded-cell',`
**Rule:** 00-FOUNDATIONS.md 8.6, the selected day in a week strip must be a filled pill shape.
**What the user sees:** The selected day highlight is a rounded square instead of the specified pill geometry.
**Fix:** Change rounded-cell to rounded-chip in the UI layer of apps/mobile/src/components/ui/week-strip.tsx.

### F-CX-R17-1-3 -- Animated.Text uses NativeWind className
**Severity:** S0
**Where:** apps/mobile/src/components/ui/rotating-micro-copy.tsx:131
**Evidence:** `      className="text-muted-foreground text-center text-sm"`
**Rule:** 00-FOUNDATIONS.md 7, never put a NativeWind className on any Reanimated Animated component.
**What the user sees:** The rotating micro-copy text may overflow its container and render as a broken overlay on unrelated UI.
**Fix:** Replace the className string with an inline style object in apps/mobile/src/components/ui/rotating-micro-copy.tsx.

### F-CX-R17-1-4 -- PersonAvatar forcibly uppercases its initial
**Severity:** S1
**Where:** apps/mobile/src/components/ui/person-avatar.tsx:29
**Evidence:** `    ? trimmed.charAt(0).toUpperCase()`
**Rule:** 00-FOUNDATIONS.md 4.1, use sentence case everywhere and never call .toUpperCase() for micro-labels.
**What the user sees:** Avatars display a forced uppercase initial, violating the strict sentence-case voice rule.
**Fix:** Remove the .toUpperCase() call from the initial derivation in apps/mobile/src/components/ui/person-avatar.tsx.

### F-CX-R17-1-5 -- Unselected ChildChip uses muted ground instead of secondary
**Severity:** S2
**Where:** apps/mobile/src/components/ui/child-chip.tsx:36
**Evidence:** `    selected ? 'bg-primary' : 'bg-muted'`
**Rule:** 00-FOUNDATIONS.md 8.4, an unselected chip must use the secondary background tint.
**What the user sees:** Unselected child chips have a darker and inconsistent background tint compared to the system standard for chips.
**Fix:** Change bg-muted to bg-secondary in apps/mobile/src/components/ui/child-chip.tsx at the UI layer.

### F-CX-R17-1-6 -- Selected ChildChip is missing font weight change
**Severity:** S1
**Where:** apps/mobile/src/components/ui/child-chip.tsx:53
**Evidence:** `          'font-medium text-sm',`
**Rule:** 00-FOUNDATIONS.md 8.4, selection requires weight and fill together, meaning selected text must jump to fontWeight 600.
**What the user sees:** The selected chip text remains medium weight instead of bumping to semibold, breaking the intended selection affordance.
**Fix:** Conditionally append font-semibold when the chip is selected in apps/mobile/src/components/ui/child-chip.tsx.

CLEAN: apps/mobile/src/components/ui/loading-indicator.tsx, apps/mobile/src/components/ui/loading-dots.tsx, apps/mobile/src/components/ui/week-bars.tsx
